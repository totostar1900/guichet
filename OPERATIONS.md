# Guichet : cahier d'exploitation

Ce que l'équipe fait au quotidien, ce qui tourne tout seul, et quoi faire quand ça coince. Aucune de ces opérations ne demande de code. Les identifiants (Supabase, Vercel, Meta, Resend) restent entre vos mains.

## 1. Qui fait quoi

| Niveau | Peut | Ne peut pas |
| --- | --- | --- |
| **Client** | lire le Guichet, déclarer des intentions, suivre des lignes, ouvrir un compte | voir le desk |
| **Opérateur desk** | valider et publier (dans la fenêtre déléguée), traiter les intentions, tenir le référentiel, saisir des cours, générer des documents | approuver hors fenêtre, gérer l'équipe, modifier la fenêtre |
| **Responsable** | tout ce qui précède + approuver / refuser (desk › Approbations), gérer l'équipe (desk › Équipe), régler la fenêtre déléguée | approuver sa propre proposition, modifier son propre niveau |
| **Système** | crons (bulletin, coupons, suivi, point du matin, émetteurs, veille des actualités, résumé du vendredi), robot WhatsApp, courriel entrant | : (jeton `CRON_SECRET` / clé service, pas un utilisateur) |

Second facteur : obligatoire pour tout accès desk. Première entrée → QR à scanner avec une application d'authentification ; ensuite un code à 6 chiffres à chaque connexion.

## 2. La journée type

| Heure (Yaoundé) | Quoi | Qui |
| --- | --- | --- |
| 07:30 | **Point du matin** arrive par e-mail (clôtures du jour, intentions de la nuit, en attente, bulletin, santé) | système → desk |
| 08:00 | desk › **Carnet › Aujourd'hui** : six tuiles (bulletin BVMAC, lignes à rafraîchir, à la une, intentions non traitées, à valider, santé) ; tout en vert, rien à faire ; une tuile orange ou rouge dit quoi faire et où ; les outils de réparation du bulletin (relancer, déposer le PDF) sont sous la tuile quand il manque ou a échoué | opérateur |
| 08:05 | desk › **Carnet du jour** : confirmer les intentions reçues, rappeler les appétits | opérateur |
| 08:15 | desk › **À valider** : sources arrivées par courriel / WhatsApp, extraction déjà faite ; corriger, cocher la liste de contrôle, publier | opérateur (relecture par un collègue si demandé) |
| dans la journée | desk › **Approbations** : décider ce qui est hors fenêtre | responsable |
| 08:30 | desk › **Actualités** : trier les liens reçus (veille de nuit, WhatsApp d'un membre, courriel), écrire les deux lignes, publier ; « À la une » si la publication concerne le plus de lignes | opérateur |
| vendredi 17:00 | **Résumé des actualités** de la semaine envoyé aux clients qui acceptent nos messages | système |
| 19:30 | **Bulletin BVMAC** ingéré automatiquement (cours, VL) ; alerte e-mail si un contrôle passe au rouge | système |
| 08:15 (lendemain) | desk › **Santé** si le point du matin signale un point orange / rouge : une carte orange ou rouge est un lien « Traiter → » vers la page où l'on agit (Cotes & VL filtrée sur les lignes sans cours, Échéanciers avec les obligations sans échéancier, Carnet filtré sur les lignes sans prix, Messages, Dépôt, Actualités, Aujourd'hui pour le bulletin) ; la page s'ouvre avec un bandeau or « Depuis Santé » qui dit quoi faire en une ligne et renvoie à la documentation (« Comment faire → ») ; les cartes vertes ne sont pas des liens. Sur Reporting, les quatre tuiles ouvrent le carnet, le journal des ordres filtré (confirmés, exécutés) ou le registre des clients | opérateur |

## 3. Publier une ligne, pas à pas

1. La source arrive seule (courriel à la boîte d'entrée, document WhatsApp d'un membre de l'équipe) ou vous la déposez (À valider › + Nouvelle source).
2. L'extraction propose un brouillon ; les champs en orange sont à vérifier, en rouge absents.
3. Choisissez le **type de produit** (il fixe les points d'attention, la liste de contrôle et les champs libres).
4. Besoin d'un second regard ? **Demander une relecture** avec une note : le brouillon passe « en revue », un collègue publie ou renvoie.
5. Fixez prix / taux et ticket minimum ; cochez la liste de contrôle ; **Publier et diffuser**.
6. Hors fenêtre déléguée : la publication part en **Approbations** ; un responsable (autre que vous) l'approuve → publication et diffusion.
7. Erreur après coup ? desk › ligne › **Historique** : comparer les versions, **restaurer** une version (motif), ou **retirer** la ligne. Rien n'est jamais supprimé.

## 4. Ce que chaque page contrôle

- **Référentiel** : types de produits, échéanciers, glossaire, sociétés, émetteurs. « Valeur par défaut » = livré avec l'application ; « modifié par le desk » = votre version publiée prend le dessus. Depuis le 21 septembre 2026, **rien ne part chez les clients sans « Publier »** : chaque enregistrement est un brouillon (étiquette orange « brouillon » sur la ligne, bandeau « n modification(s) non publiée(s) » en tête de l'onglet avec « Publier » et « Abandonner les brouillons ») ; les fiches, listes et simulateurs lisent la valeur publiée. Dans le panneau d'une entrée : « Publier cette entrée », « Abandonner ce brouillon », et le vrai bouton « Revenir aux valeurs par défaut » (confirmation, puis un brouillon ↺ qui efface la version du desk à la publication ; une entrée créée par le desk se « supprime » de même). Chaque formulaire a « Annuler » (retour à la liste, panneau fermé) ou « Effacer » (nouvelle entrée) ; après un enregistrement, la liste revient avec la ligne surlignée et le message « enregistré en brouillon ». L'onglet Échéanciers montre une ligne par obligation cotée : en orange celles sans échéancier exact (le prix court sur l'année du bulletin, « Créer l'échéancier » pré-rempli sur la ligne, un bandeau en tête compte et explique), en gris les échéanciers de lignes échues ou retirées (« hors bulletin », gardés) ; boîte de recherche (ISIN, émetteur, ligne, source, sans accents, avec suggestions), menu d'état (sans échéancier, défaut, desk, brouillon, hors bulletin), tri par colonne ; Santé › « Obligations sans échéancier » ouvre le tableau filtré. Sociétés cotées et Émetteurs : la fiche est un formulaire (identité, capital et actionnaires, chiffres par exercice, documents, lecture ; lignes à ajouter / retirer ; la fiche JSON reste visible repliée), plus jamais un bloc JSON à taper. Une société ou un émetteur nouveau n'est pas créé à la main : sa ligne vient du bulletin, l'onglet liste « n société(s) cotée(s) au bulletin sans fiche » avec « Créer la fiche » pré-remplie (identifiant, ISIN, nom, pays) ; « Dupliquer » sur une ligne part d'une fiche voisine (identifiant suffixé, nom marqué « (copie) », ISIN à donner). Leçons : « Dupliquer » ouvre une nouvelle leçon remplie comme l'originale (clé suffixée, ordre suivant, titre « (copie) »), une clé déjà prise est refusée. Le bouton « Importer les valeurs par défaut » n'existe plus : les valeurs livrées se lisent sans être copiées. Base : migration 0031 (`reference.data` nullable, `draft`, `draft_by`, `draft_at`) ; brouillon = `{op:"set",data}` ou `{op:"reset"}`.
- **Journal** : la piste d'audit : qui, quoi, avant / après, motif, adresse. « Chaîne intègre » doit toujours être vert ; sinon, prévenez le responsable et gardez une capture.
- **Équipe** : donner / retirer l'accès. La personne doit s'être connectée une fois au Guichet avant. `DESK_EMAILS` ne sert qu'au tout premier responsable.
- **Approbations** : file des propositions hors fenêtre et réglage de la fenêtre (prix OTA/APE, taux BTA, écart de cours, frais de fonds).
- **Actualités** (Marché) : des liens vers ce que d'autres publient (Trésors, BVMAC, COSUMAF, presse, sociétés, sociétés de gestion), jamais l'article. Une publication = lien + titre reformulé + « Pourquoi ça compte » en deux lignes sans recommandation (les mots « achetez », « garanti », « à ne pas manquer » sont refusés) + lignes et notions liées (elle s'affiche alors sur leur fiche) + visibilité (30 jours par défaut). Une seule « à la une ». Trois entrées : le formulaire (« Lire la page » remplit titre, source, date), un lien envoyé au numéro WhatsApp depuis un téléphone de l'équipe, un courriel d'un expéditeur de confiance. Chaque version est journalisée (entité Actualités du Journal) ; un lien mort est signalé sur Santé. Table `news` (migration 0025) : seules les publications sont lisibles hors du desk.
- **Documentation** (Pilotage, `/desk/docs`) : cinq pages FR/EN (comment fonctionne Guichet, plateformes et coûts, aider un client, administrer, aperçu technique) avec recherche par chapitre, filtres par public et plan de page. Le texte vit dans `src/data/docs/*.ts` ; chaque page porte « vérifié le » et son responsable : un écran qui change fait relire sa page dans le même envoi. Une seule page est publique (`visibility: "public"`) : l'**Aide** des clients sur `/info/aide`, écrite pour eux seuls ; un test refuse tout détail interne (services, clés, /desk, procédures) dans une page publique. L'Aide se termine par « Comment Guichet est entretenu » : qui écrit, d'où viennent les cours, les mises à jour, les pannes, les données : en clair. **Notes de travail** (`/desk/docs/notes`) : les notes que l'assistant garde entre ses sessions, copiées dans le dépôt sous `docs/notes/*.md` et lues à chaque visite ; elles disent *pourquoi* les choses sont faites ainsi (en anglais, réservées au desk).
- **Le Guide** : deux parcours. « Lire une ligne en trente secondes » (huit leçons) et « Comprendre le marché CEMAC » (`/info/parcours` : cinq sections repliables, acteurs, instruments, risques, passer un ordre, fiscalité et frais, vingt leçons de deux minutes, une barre de progression par section, la section en cours rouverte au retour). Une leçon = titre, accroche, trois paragraphes, un schéma (carte des acteurs, chemin d'un ordre, ligne de vie d'un titre, catégories de fonds) ou un bloc à manipuler, une question, les mots du glossaire. Le desk écrit et corrige les leçons dans le Référentiel › Leçons (colonne « Cours » = le parcours et sa section) ; les sections, leurs formes et leurs couleurs sont dans le code (`src/data/parcours.ts`), les illustrations des dix acteurs dans `src/components/Illustrations.tsx`. Le glossaire a sa propre recherche, un tri et un regroupement par catégorie (catégories fixées par clé dans `src/app/info/Glossary.tsx` ; un mot nouveau sans catégorie tombe dans « Autres ») et contient les dix états d'une ligne (À venir … Sur demande, clés `etat_*`), les mêmes mots que les pastilles. Règle pour une leçon : une notion, deux minutes, une accroche, trois paragraphes courts, un schéma ou un bloc sur une vraie ligne, une question, les mots du glossaire ; un schéma seulement quand la notion est une relation (qui fait quoi) ou un chemin (dans quel ordre).
- **Côté client, la navigation** : le menu choisit la page : **Titres** (obligations, bons du Trésor, actions ; deux interrupteurs Marché primaire / Marché secondaire, tous deux allumés par défaut, en éteindre un ne garde que l'autre) et **Fonds** (un tableau sur un écran large, des cartes sur téléphone, les quatre catégories expliquées dans un bandeau repliable), chacun avec son compteur. Les listes (Titres, Fonds) gardent leurs filtres, leur tri et leur position d'écran dans l'adresse et sur l'appareil ; la fiche d'une ligne ramène à la liste telle qu'elle était et permet de passer à la ligne précédente / suivante dans le même ordre. Le comparateur choisit chaque ligne dans un sélecteur avec saisie, marché et instrument. Le **Guide** (`/info`, ex-« Info ») et l'**Aide** (`/info/aide`) ont un sommaire à gauche, fixe sous l'en-tête, qui marque la section lue ; la recherche du Guide comprend une question (« c'est quoi un coupon »), les pluriels et quelques synonymes courants, et un résultat dans la page fait défiler jusqu'à l'endroit et l'éclaire un instant. Les fonds s'appellent « OPCVM » dans les bandes de marché (« Investment funds » en anglais). La fiche d'un fonds montre une VL par point sur le graphique, avec une bulle (VL, date, bulletin, variation, depuis le début de la période, depuis l'origine) au survol ; la période se choisit (1 mois … 3 ans, origine, ou deux dates ramenées à la VL publiée la plus proche ; gardée dans l'adresse) et un bloc « Sur la période choisie » calcule performance, annualisée, plus haut / plus bas, VL en baisse, plus forte baisse et le devenir de 1 000 000 FCFA placés ; les quatre chiffres du bulletin restent en dessous. Cinq lectures des mêmes VL par onglets (VL, rendement annualisé avec fenêtre 3 / 6 / 12 mois et le dernier BTA en repère, 1 000 000 FCFA placés, variations, repli), la lecture et la fenêtre dans l'adresse (`graphe`, `fenetre`). Dans le tableau du comparateur, un fonds a deux lignes de rendement : « Rendement » (12 mois, ou par an depuis l'origine) et « Depuis l'origine » avec la date de création et l'âge du fonds : et des bulles « i » sur VL, Variation et Depuis l'origine (terme de glossaire `perf_origine`, modifiable dans le Référentiel). Sur téléphone, les graphiques dessinent leurs étiquettes plus grandes, la bulle se ferme d'un toucher hors du graphique, et les textes « Comment lire » du comparateur ne parlent que de ce que la paire contient. Le comparateur ajoute, sous le tableau, des graphiques selon la paire (rendement sur une règle pour toutes ; deux fonds sur leur période commune ; calendrier des flux de deux titres de dette ; passé d'un fonds face à la promesse d'un titre), chacun avec un encadré « Comment lire » : les textes vivent dans `src/components/CompareCharts.tsx`, le modèle dans `src/lib/domain/compare.ts`.
- **Côté client, sur téléphone** : les gestes ont été décidés un à un le 19 septembre 2026. Sur une fiche, glisser vers la gauche ou la droite passe à la ligne suivante ou précédente de la liste d'où l'on vient (la voisine apparaît sous le doigt avec son nom, la page se cale au tiers de l'écran, « Relâchez pour ouvrir ») ; la première fois sur un appareil, la fiche fait un pas de côté pour le montrer. Sur une liste, tirer une carte vers la gauche découvre « Déclarer » (le formulaire d'intention de la fiche) et « Me rappeler » (le même formulaire, rappel choisi), un long tirage déclare d'un coup ; la tirer vers la droite la retourne : au dos, bleu pâle, le calendrier de l'opération avec l'étape en cours (ou, pour une ligne cotée ou un fonds, prochain coupon, dernier dividende, prochaine VL), la courbe quand la ligne a un passé (soixante derniers cours ou VL, rendements servis aux adjudications passées du même Trésor), et le calcul de référence de la fiche ; en vue compacte, les quatre chiffres de la liste ouvrent le dos ; une carte retournée reste retournée tant qu'on la lit : on revient par son bouton « ↺ Recto », par un demi-tour en sens inverse, en retournant une autre carte (une seule à la fois) ou quand elle sort de l'écran ; les cartes ne portent aucune couche 3D au repos (cinquante cartes composées en permanence faisaient planter Safari sur iPhone) ; il n'y a pas de glissé de page entre Titres et Fonds (les onglets du bas y mènent), ni d'appui long, ni de « tirer pour rafraîchir » maison (Android l'a d'origine, iPhone non). Le « ··· » (trois points or) d'une carte, d'une ligne de tableau ou de la fiche ouvre une feuille à en-tête navy : voir la fiche, suivre / ne plus suivre, comparer avec, partager le lien, copier l'ISIN, fiche PDF ; jamais « Déclarer une intention », qui reste un bouton. Une carte porte le statut et le « ··· » dans son coin, le chiffre, trois faits (la durée sous la date d'échéance), la clôture et « Voir la fiche » en pied. Deux icônes à droite du tri choisissent des cartes détaillées ou compactes, et le bouton « Affichage des cartes » à côté ouvre les réglages : densité et huit façons de distinguer une carte de la suivante (aucune, alternance, demi-trait ou trait de la couleur de famille, bande gauche, teinte, ombre, alternance + demi-trait ; « alternance » par défaut), gardés sur l'appareil pour les titres et les fonds. Les bandes de filtres des titres et des fonds ne sont plus figées : sorties de l'écran, une icône flottante en bas à gauche ouvre les mêmes réglages (filtres, recherche, tri) dans une feuille sans bouger la page ; un chevron en bas à droite remonte en haut des listes, du Guide et du parcours ; chaque liste retrouve ses filtres et sa position quand on y revient. Une ligne partagée (WhatsApp, courriel, réseaux) montre une image dessinée à la demande avec ses chiffres (titre, émetteur, rendement ou VL, statut, ISIN) et une description qui les répète ; WhatsApp garde un aperçu quelques jours, ajouter `?v=1` au lien force le nouveau.
- **Le « ⋮ » de l'application** (décidé le 19 septembre 2026, inspiré de WhatsApp, Revolut et Notion) : trois points verticaux en haut à droite de chaque page, pour tout le monde ; le « ··· » or horizontal d'une carte reste celui de la ligne. Même feuille à en-tête navy sur téléphone, menu déroulé sous l'en-tête sur grand écran. Côté client, dans l'ordre : **Parler à quelqu'un** (WhatsApp vers le numéro de la société, prérempli avec la ligne depuis une fiche, appel, e-mail), **Découvrir** (« Comment lire cette page ? » quand la page a des repères, « Guichet en trente secondes », « Premiers pas », Aide, Le Guide), **Réglages / Mon compte** (langue, affichage des cartes, alertes, Sécurité avec le compte des canaux prouvés et des appareils, Se déconnecter), et un pied avec l'agrément, « À propos » (le chapitre « Comment Guichet est entretenu ») et le numéro de version. Côté desk : visite guidée, « Cette page, champ par champ » (le Guide ouvert sur la page courante), Documentation, « Ce que voit le client » (les deux animations d'accueil), langue, Se déconnecter. Un dernier repère, une fois par appareil après les deux animations, montre le « ⋮ ». Code : `src/components/AppMenu.tsx`.
- **Le Guide et l'Aide, sur téléphone** (20 septembre 2026) : chaque page du Guide (leçons, parcours, aide, mentions) porte en bas à droite une capsule (le « double bouton », décidé le 20 septembre) : depuis une leçon, la pastille du chapitre (sa lettre dans sa couleur, l'anneau de l'avancement du chapitre, « 4 / 8 ») et le sommaire, deux moitiés qui ouvrent la même feuille à hauteur fixe sur l'onglet qui va avec (« Ce chapitre » : « Vous êtes ici », les leçons du chapitre cochées, précédente / suivante en pied ; « Tout le Guide » : recherche, les deux parcours et leur avancement, les outils ; un balayage horizontal passe d'un onglet à l'autre) ; pendant le défilement la capsule se replie sur la pastille, le sommaire en badge sur son épaule. Hors leçon, le sommaire seul, la feuille sans onglets avec « Vous êtes ici » en toutes lettres. Sur grand écran, le sommaire de gauche fait ce travail. La carte des acteurs (leçon A1) se lit sur téléphone en trois bandes empilées, « Agrandir » l'ouvre en plein écran (pincer, glisser, double toucher). **Les mots s'expliquent sur place** : chaque acteur de la carte (BEAC, COSUMAF, BVMAC, dépositaire, Trésor, SVT, société de gestion, société de bourse, émetteur, compte-titres) a son entrée au glossaire (catégorie « Les acteurs du marché ») et un toucher, dans la carte comme dans le corps d'une leçon (BTA, OTA, APE, SVT, adjudication, rachat, introduction en bourse…), ouvre une feuille avec le dessin, le sigle développé, le rôle, puis « Voir dans le glossaire » et la leçon qui en parle. Code : `src/components/TermSheet.tsx`. **La ligne « Sur cette page »** (20 septembre) remplace sur téléphone la rangée de puces qui glissait sous l'en-tête (Guide, Aide, parcours, documentation du desk) : le nom de la section lue en toutes lettres, sa place (2 / 6, ou « 0 / 4 lues » sur le parcours avec la lettre dans sa couleur), un fil de segments ; toucher ouvre la liste numérotée dans une feuille, avec « Haut de page » et « Section suivante ». Sur les listes (Fonds, Sociétés, Actualités, et déjà Titres), la rangée de filtres devient « Filtres · n » avec le résumé des choix en clair et « Tri », qui ouvrent la feuille des filtres. Trait de maison : tout lien ou mot qui ouvre quelque chose porte un simple pointillé fin (règle `a` de globals.css). La ligne « Alertes sur cet appareil » du menu n'apparaît que si les clés VAPID sont posées sur Vercel. **Le profil financier est ouvert aux visiteurs** (20 septembre) : `/moi/profil` sans compte donne le même questionnaire, à titre d'information ; le profil reste sur l'appareil (`guichet:profile`), la page le dit en tête et propose « Garder ce profil dans mon dossier » (connexion) ; une fois connecté, un profil trouvé sur l'appareil est proposé en une ligne « Le garder ». Entrées : la tuile « Mon profil » du ⋮ pour tout le monde, une tuile « Votre profil financier en deux minutes » dans « Outils et repères » du Guide. **Profil version 2** (20 septembre) : dix-sept questions en trois blocs, « ce que vous pouvez supporter » (horizon, objectif, tolérance, réserve), « ce que vous connaissez » (expérience, instruments détenus, fréquence, puis quatre vérifications liées à des leçons : prix et coupon, adjudication, dépositaire, VL ; une réponse fausse montre la bonne et la leçon), « ce que vous pouvez engager » (revenus, épargne à placer cette année, part, réserve, engagements, situation, en fourchettes). Résultat : le mot (tenu à « équilibré » quand la capacité est limitée, avec la phrase qui le dit), niveau de connaissance (débutant · informé · averti), capacité (limitée · moyenne · bonne), confiance (déclaré · vérifié : trois vérifications justes ou huit leçons lues). Sur la fiche, une intention dont le décaissement dépasse la moitié de l'épargne déclarée demande une confirmation, envoyée au desk avec le drapeau. Le desk voit le profil sur la page de l'intention et dans le dossier client (`ProfileCard`) : mot, connaît, peut engager, part du montant, « à couvrir à l'appel » avec les leçons. Le ⋮ du téléphone n'a plus qu'une feuille : recherche, tuiles, FR · EN et Se déconnecter, « Nous joindre » en pied. **Mes documents** (Mon espace) se lisent par opération (un groupe par intention, titré par la ligne, le type, le montant, la référence et l'état ; les documents dans l'ordre où ils sont arrivés ; les pièces du dossier et relevés dans « Mon dossier ») ou par date (chaque ligne rappelle l'opération) ; le choix reste sur l'appareil, « par opération » par défaut dès deux opérations. Sur téléphone, historique et documents se lisent en cartes (la ligne, le type · montant · réf. · date, l'état, la suite ; le document, son numéro et sa date, l'état, le PDF), le tableau restant sur grand écran. Une ligne retirée de la liste (repassée en brouillon, retirée) garde son nom et sa fiche dans l'historique : la page va la chercher une à une (`getOffer`), et la base interdit d'effacer une ligne qui porte des intentions. **Le Guide se replie** : chaque section (leçons, parcours, simulateur, outils, mots) sous un trait or fin avec un chevron, « Tout replier » sous la recherche ; l'état reste sur l'appareil, un lien vers une section la rouvre (`src/components/Fold.tsx`). Mon espace se replie de même, avec sur chaque titre un chiffre et un mot d'état (« 2 · une à signer », « 5 M · flux le 14 févr. », « WhatsApp ✓ · e-mail ✓ ») : replié, la page est un tableau de bord ; l'historique est replié par défaut. **Les couleurs** (20 septembre) : dans le ⋮, « Couleurs » propose quatre palettes, toutes proches de la marque ou neutres (Navy et or par défaut, Ivoire : navy et or sur papier chaud, Ardoise : neutre bleuté, Encre : noir et gris chauds, l'or seul), et le thème auto · clair · sombre ; `src/app/palettes.css` redéfinit les jetons par `data-palette`, le choix reste sur l'appareil et s'applique avant le premier rendu (script inline du layout). Ce qui ne change jamais : l'or de la sélection, les états, les familles, les sections du parcours. Contraste vérifié le 20 septembre dans les huit combinaisons (palette × thème, script dans la console du navigateur sur les paires encre/papier, surfaces, zébrures, sélection, familles, courbes, lignes) : texte 4,5:1, éléments 3:1 partout, seul l'or décoratif sur le papier reste sous 3 (jamais porteur de texte). Règle : les textes pâles sur navy prennent `--on-navy-2`, jamais un hexa ; la piste de la jauge du profil est un blanc translucide. **Le choix tient d'une page à l'autre** (20 septembre, tard) : palette et thème s'écrivent aussi en cookies (`guichet_palette`, `guichet_theme`), le serveur pose `data-palette` / `data-theme` sur `<html>` lui-même (`paletteAttrs`, `src/lib/palette.ts`) ; sans cela React remettait les attributs d'origine à chaque re-rendu du layout (changement de page, de langue). `PaletteKeeper` les repose après chaque navigation par sécurité ; localStorage reste la vérité, le script de démarrage aligne les cookies. **Thème « tamisé »** entre clair et sombre (`data-theme="dim"`) : un vrai entre-deux lu avec les encres claires, une page bleu-gris (papier #dbe1ea), navy et or comme en clair, l'or d'encre assombri ; Ivoire tamisé est une crème chaude, Ardoise un gris bleuté, Encre un gris chaud. Le sombre ne bouge pas. Règle CSS : les sélecteurs du sombre automatique excluent `dim` (`:not([data-theme="light"]):not([data-theme="dim"])`). Audit des douze combinaisons (4 × clair · tamisé · sombre) : tout passe. **Visites guidées** (20 septembre, tard) : la visite du desk lancée depuis `/desk` (la page de sa première étape) ne démarrait pas, un push vers le même chemin ne changeant rien ; `startDeskTour` envoie l'événement `guichet:desktour:start` quand on y est déjà. Sur téléphone, une étape dont la cible est absente de la mise en page est racontée sans anneau (sauf si une autre étape de la page peut être montrée), pour le desk comme pour les repères d'une page. Sous les barres navy, un trait or (`--bar-edge`) dès que la page est sombre, pour que l'en-tête et la barre d'onglets se détachent. Les vignettes de palette sont de petites pages (papier, bande navy, pastille or, deux lignes d'encre). Les couleurs de catégorie des fonds sont des jetons (`--cat-M/O/D/A`) avec leur version sombre. Mes documents : chaque opération se replie depuis sa tête, « Tout replier / déplier » dans la barre. **Le compte** (initiale en haut à droite du téléphone) ouvre une feuille à la manière de l'app Claude ; la règle : l'initiale, c'est vous, le ⋮, c'est l'app. Depuis le 20 septembre au soir : la carte (nom et ville corrigés sur place par « Modifier », le segment garde son premier mot ; `identityAction`), les deux canaux avec leur preuve (« prouvé » ou « à prouver », vers Sécurité), « Chez vous » (Mon espace ou Le desk, Mon profil financier, Sécurité, Mes coordonnées et pièces avec l'état du dossier KYC), « Préférences » (langue, alertes sur cet appareil si les clés VAPID sont posées, « Comment vous joindre d'abord » WhatsApp · E-mail · Appel, « Relevés par e-mail » ; `prefsAction`, colonne `profiles.prefs` jsonb, migration 0029 appliquée le 20 septembre), « Bientôt » (compte-titres, proches, inviter, inertes), et en pied « Se déconnecter de cet appareil » et « Se déconnecter partout » (`logoutEverywhere`, signOut global). Le ⋮ ne garde la langue et « Se connecter » que pour un visiteur. Le desk lit la préférence : sous l'identité du dossier client et sur l'intention, une ligne de pastilles (`ReachLine`) dit par quel canal commencer (le choix du client, sinon le canal prouvé « par défaut »), l'état de preuve de WhatsApp et de l'e-mail, et si les relevés partent par e-mail. **Connexion par e-mail** : avec `RESEND_API_KEY` + `EMAIL_FROM` sur Vercel, le Guichet envoie lui-même l'e-mail : le code à six chiffres en grand (celui que Supabase frappe via `generateLink`) et un lien qui s'ouvre partout ; le formulaire dit « saisissez le code ». Sans ces clés, le lien Supabase part en flux implicite (voir `docs/supabase-email-templates.md`) et s'ouvre depuis n'importe quel navigateur ; le formulaire parle alors du lien. État au 20 septembre 2026 : clés posées, code vérifié en production (Resend › Emails : « Votre code Guichet : … », ouvert). L'expéditeur est encore `onboarding@resend.dev`, qui n'atteint que l'adresse du compte Resend : Fait le 20 septembre en fin de journée : le domaine `purposecapital.africa` est vérifié chez Resend (DKIM, SPF sur `send`, DMARC) et `EMAIL_FROM` vaut « Guichet · Purpose Capital <guichet@purposecapital.africa> » ; le code atteint toute adresse. La zone DNS du domaine est servie par **Netlify DNS** (nameservers NS1 `dns1..4.p05.nsone.net`, SOA netlify) : c'est là que se posent les enregistrements, pas chez GoDaddy (registrar) ni Namecheap. L'Aide est une page qui répond : la question d'abord (recherche en place, les trois questions les plus posées en puces), six sujets en tuiles, chaque réponse s'ouvre en place avec son bouton, « Cette réponse vous a aidé ? » écrit au journal (entrée « Aide · … »), un conseiller en bas. Le « ⋮ » a deux onglets sur téléphone, à hauteur fixe pour que le passage de l'un à l'autre ne fasse rien bouger : « Guichet » (recherche, repères de la page, six tuiles : Trente secondes, Premiers pas, Aide, Le Guide, Mon profil, Sécurité ; FR · EN et Se déconnecter sur une ligne, les alertes) et « Contact » (WhatsApp en navy avec le numéro en grand, Appeler, E-mail, l'adresse et l'agrément, les horaires et « en ligne maintenant » aux heures d'ouverture) ; sur grand écran, « Aide » et « Réglages ». L'affichage des cartes se règle depuis les listes elles-mêmes. Son index vient de `/api/guide-index` (glossaire, leçons, questions de l'aide, outils, pages), construit dans `src/lib/guide-index.ts`.
- **Profil financier** (`/moi/profil`, `src/data/profile.ts`, migration 0028) : sept questions, un profil prudent / équilibré / dynamique, quatre mesures ; « dans / au-delà de votre horizon » sur la fiche ; une intention hors profil est cochée par le client et lue par le desk (« Hors profil, confirmé par le client ») ; jamais un conseil, aucune ligne cachée. Le journal garde le jour, pas les réponses.
- **Mentions et responsabilités** (`/info/mentions`, texte dans `src/data/legal.ts`) : qui parle, ni conseil ni promesse, les risques, les canaux et les données, la réclamation. Un client l'accepte **une fois par version** (la date en tête du texte) à sa première page après connexion ; changer le texte = changer `LEGAL_VERSION`, et chacun le réaccepte à sa connexion suivante. Colonnes `terms_version` / `terms_accepted_at` sur `profiles` (migration 0027), ligne « Mentions acceptées » au journal. Le desk n'a pas la boîte.
- **Côté client, connexion et canaux** (décidé le 19 septembre 2026) : pas de mot de passe, un code à six chiffres par e-mail. Une intention part avec ses **deux canaux prouvés** : l'e-mail par la connexion, le numéro WhatsApp par un code à six chiffres envoyé dessus et saisi dans le formulaire (dix minutes, cinq essais, une seule fois par numéro ; sans WhatsApp configuré, le code s'affiche dans le formulaire comme démonstration). Un invité donne son e-mail sur la fiche : le code le connecte et crée son espace, puis il prouve son numéro ; ou il écrit d'abord sur WhatsApp et le desk répond depuis Messages avec « **Répondre avec la ligne** » : un lien signé pour ce numéro (trente jours) qui, ouvert depuis son téléphone, reconnaît le numéro et ne demande plus que le code e-mail. Un nouveau numéro ou une nouvelle adresse perd sa preuve (Mon espace › Sécurité la refait). La fiche d'une intention montre au desk si les deux canaux étaient prouvés. **Sécurité** (`/moi/securite`) : les deux canaux et les **appareils de confiance** : la serrure du téléphone (clé d'accès WebAuthn : Face ID, empreinte, code du téléphone) ou un **code à quatre chiffres lié au navigateur** (le navigateur garde un jeton, le serveur le haché du jeton et du code : le code seul n'ouvre rien ailleurs, cinq codes faux oublient l'appareil) ; l'appareil connu ouvre d'abord sur la page de connexion (« Bonjour … »), le code e-mail reste dessous ; chaque ajout et retrait est annoncé au client sur ses deux canaux et inscrit au journal (entrée « système »). Tables `channel_codes` et `trusted_devices`, colonnes `phone_verified_at` / `email_verified_at` sur `profiles`, `phone_verified` / `email_verified` sur `intents` (migration 0026). `AUTH_SECRET` sert de sel à tout cela : le changer invalide les codes en cours, les liens signés et les codes à quatre chiffres (les clés d'accès restent).
- **Langues** : l'application est en français et en anglais (bouton FR · EN, choix gardé sur l'appareil). Tout texte d'écran est écrit en français dans le code et traduit par un dictionnaire ; un texte sans traduction reste en français plutôt que de casser la page. Ce que le desk écrit (titres d'actualités, deux lignes de lecture, notes du référentiel) reste dans la langue où il l'a écrit, sauf s'il renseigne la version anglaise du formulaire. Un audit automatique des 49 pages en anglais est lancé avant une mise en production qui touche aux textes (dernier : 18 septembre 2026, aucun texte d'écran en français).
- **Guide** (`/desk/guide`) : chaque page du desk champ par champ, des captures (43 fichiers `public/guide/*.png`, en deux passes : `GROUP=desk npm run guide:shots` sur `npm run dev:memory` pour les pages du desk, qui ont besoin du jeu de données de démonstration et de la session de développement ; `GROUP=client npm run guide:shots` sur `npm run dev`, donc sur les vraies lignes et les vrais fonds, pour les pages du client ; `ONLY=cle,cle` pour quelques captures ; Next refuse deux serveurs de développement dans le même dossier, arrêter l'un avant l'autre ; neuf captures sont à la largeur d'un téléphone, dont une carte tirée et une carte retournée, pilotées par des touchers simulés) ; une galerie possible par section : Marché montre la page Titres et la page Fonds sur grand écran puis le téléphone (cartes détaillées, compactes, tirée, retournée, fonds, dos d'un fonds, le « ··· », « Affichage des cartes », la fiche) ; Documentation montre l'index, les six pages, l'Aide côté desk et côté client, le chapitre « Comment Guichet est entretenu », la page Guide (/info), l’écran des premiers pas et les notes de travail ; Référentiel montre le Guide, l’Aide et ce chapitre) et la visite guidée (20 étapes, `TOUR` dans `src/data/desk-guide.ts` ; une étape peut porter un lien et un aperçu). Côté client : « Guichet en trente secondes » (cinq écrans animés de six secondes, ce qu'est Guichet puis voir · dire · parler · suivre ; sur téléphone une story, toucher à droite avance, à gauche recule, un doigt maintenu met en pause ; sur grand écran deux colonnes ; une fois par appareil, avant les premiers pas, rejouable depuis le Guide et sous le formulaire de connexion), puis « Premiers pas » (six écrans, rejouable depuis le Guide), et des repères sur la fiche, le Guide, Actualités et Aide.

## 5. Brancher la boîte d'entrée (courriels → À valider)

Le point d'entrée est `POST https://<domaine>/api/inbound/email` avec `Authorization: Bearer <INBOUND_SECRET>`. Il accepte un message brut (`Content-Type: message/rfc822`) ou un JSON `{ from, subject, text, attachments:[{ filename, contentType, contentBase64 }] }`. Chaque PDF / image devient une source ; un expéditeur de `INTAKE_TRUSTED_SENDERS` (domaines, adresses, numéros) arrive « officiel ».

Option gratuite, sans serveur à gérer : **Cloudflare Email Routing** sur votre domaine :

1. Cloudflare › Email › Email Routing : activer, créer l'adresse `intake@<domaine>`.
2. Workers › créer un Worker avec ce code, variables `GUICHET_URL` et `INBOUND_SECRET` :

```js
export default {
  async email(message, env) {
    const raw = await new Response(message.raw).arrayBuffer();
    const r = await fetch(`${env.GUICHET_URL}/api/inbound/email`, {
      method: "POST",
      headers: { authorization: `Bearer ${env.INBOUND_SECRET}`, "content-type": "message/rfc822" },
      body: raw,
    });
    if (!r.ok) message.setReject(`Guichet a refusé le message (${r.status})`);
  },
};
```

3. Email Routing › Routes : `intake@<domaine>` → *Send to a Worker* → ce Worker.
4. Vercel : ajouter `INBOUND_SECRET` (même valeur) et `INTAKE_TRUSTED_SENDERS`, redéployer.
5. Test : envoyez un PDF de communiqué à `intake@<domaine>` ; il apparaît dans À valider en moins d'une minute.

WhatsApp : dès que `WHATSAPP_TOKEN` / `WHATSAPP_PHONE_ID` sont renseignés, un document ou une photo envoyés au numéro de la société **par un membre de l'équipe** (numéro renseigné dans son profil) devient une source ; d'un client, il est seulement signalé dans le flux.

## 6. Mettre à jour l'application sans casser la production

- Chaque `git push` sur `master` déploie la production. Toute autre branche déploie une **prévisualisation** (URL `guichet-git-<branche>-….vercel.app`, protégée par connexion Vercel) : c'est là qu'on regarde avant de fusionner.
- Règle : une modification = une branche = une prévisualisation regardée par un responsable = fusion sur `master`. Sur GitHub, *Settings › Branches › Add rule* sur `master` : « Require a pull request before merging » (1 relecteur) rend la règle obligatoire.
- Les migrations SQL nouvelles (`supabase/migrations/00NN_*.sql`) se jouent à la main dans Supabase › SQL Editor **avant** de fusionner le code qui les utilise. Une valeur d'enum (`alter type … add value`) se joue seule, dans son propre run.
- Retour arrière : Vercel › Deployments › déploiement précédent › *Promote to Production* (30 secondes). Les données ne sont pas touchées. **Attention** : après un retour arrière (*Instant Rollback* ou *Redeploy* d'un ancien déploiement), Vercel cesse de promouvoir automatiquement les déploiements suivants : ils se construisent, passent « Ready » mais restent **Staged** (le badge *Production* reste vide, la fiche dit « Production · Staged », « Assigning Custom Domains : Skipped »), et le site continue de servir l'ancienne version quoi qu'on pousse. Pour reprendre : Deployments › le déploiement voulu › *Promote* : la promotion automatique repart ensuite pour les pushes suivants. Vécu le 19 septembre 2026 : un *Redeploy* de `0ef4768` a figé la production près d'une heure ; la promotion de `5087cc8` a tout remis en route. Avant de faire un retour arrière, préférer dire ce qui ne va pas et corriger en avant.
- La file de construction du plan Hobby est lente les soirs de nombreux pushes (10 à 40 minutes entre le push et le début de la construction, un seul chantier à la fois ; Vercel peut annuler de lui-même un build dépassé par un push plus récent). Vérifier sans le tableau de bord : `https://api.github.com/repos/totostar1900/guichet/commits/<sha>/status` (state `pending` = pas encore construit, `success` = construit ; « Canceled from the Vercel Dashboard » = annulé) et un `curl` de la page pour y chercher un marqueur du nouveau code.
- Tâches planifiées (`vercel.json`, jeton `CRON_SECRET`, UTC) : `boc` 18:30 lun–ven (bulletin BVMAC), `coupons` 07:00, `suivi` 07:15 (lignes suivies, point du matin), `point` 06:30 lun–ven (opportunités en file), `emetteurs` lundi 06:00 (documents des sociétés), `actualites` 04:00 (veille des flux, vérification des liens), `actualites-hebdo` vendredi 16:00 (résumé aux clients). Sur Vercel, le plan Pro est nécessaire (sept tâches, fonctions jusqu'à 5 min, usage commercial).

### L'échelle d'espacement

- Les espaces ont un nom depuis le 23 septembre 2026 : `--s-1` à `--s-11` dans `src/app/globals.css`, soit 2 · 4 · 6 · 8 · 10 · 12 · 16 · 20 · 24 · 32 · 40 px. Elle décrit ce qui était déjà posé : 76 % des espaces de l'application tenaient déjà sur six de ces pas.
- Le retrait intérieur d'un panneau appartient au panneau (`--panel-inset`) : une feuille de page ne le redit pas, elle ne pose que le vertical, avec `padding-block`.
- 14 px et 18 px sont les deux pas hérités : ils marchent, ils ne s'écrivent plus dans du code neuf.
- Le garde-fou : `src/test/spacing.test.ts` compte ce qui sort de l'échelle et refuse que le compte augmente. Il peut descendre quand une feuille se range, il ne remonte pas. Si le test échoue sur un travail légitime, poser un pas de l'échelle ; si la valeur est vraiment nécessaire, baisser le plafond du test dans le même envoi, en disant pourquoi.
- Harmoniser n'est pas uniformiser : le desk se balaie et prend les petits pas, une note de marché se lit et prend les grands. La même échelle, pas les mêmes degrés.

## 7. Quand ça coince

| Symptôme | Quoi faire |
| --- | --- |
| « Modifié entre-temps (version n) » à l'enregistrement | Quelqu'un a enregistré avant vous : rechargez, relisez, recommencez. Rien n'a été écrasé. |
| Point du matin absent | desk › Santé (le cron a-t-il tourné ?), puis Vercel › Cron Jobs › `point` › Run. E-mail non configuré : le point est quand même dans le flux du desk. |
| Bulletin non ingéré | desk › Marché › Ingérer le bulletin (date du jour). Le site BVMAC peut être en retard : réessayer plus tard, ou déposer le PDF. |
| L'e-mail de connexion contient un lien et pas de code | Sur l'expéditeur par défaut de Supabase les gabarits ne se modifient pas : activer le SMTP Resend dans Supabase puis coller les gabarits de `docs/supabase-email-templates.md` (dix minutes, titulaire du compte Supabase). |
| Un client arrive sur la page d'accueil avec « lien invalide ou expiré » dans l'adresse | Le lien de connexion a expiré ou a déjà servi (Mail sur iPhone ouvre les liens en avance) : l'application le renvoie sur la page de connexion avec l'explication ; lui conseiller le code à six chiffres plutôt que le lien. |
| Un membre a perdu son téléphone | Équipe › retirer l'accès, puis le redonner : il réactive son second facteur à la connexion suivante. |
| Un client a perdu son téléphone, ou doute d'un appareil | Lui : Mon espace › Sécurité › Oublier l'appareil (depuis n'importe quel navigateur, après le code e-mail). Vous : Supabase › Table `trusted_devices` › supprimer ses lignes ; le client se reconnecte par le code e-mail. Un ajout d'appareil qu'il ne reconnaît pas lui a été annoncé sur ses deux canaux : traiter comme une compromission de la boîte e-mail. |
| « Ce numéro WhatsApp n'est pas encore prouvé » à l'envoi d'une intention | Le client n'a pas saisi le code reçu sur WhatsApp (étape 2 du formulaire). Sans WhatsApp configuré sur ce serveur, le code s'affiche dans le formulaire. Le numéro a changé : la preuve tombe, un nouveau code repart. |
| « Le code WhatsApp n'est pas encore disponible sur ce serveur » | Ni le code par téléphone de Supabase (`PHONE_OTP_ENABLED=1`, Auth › Phone avec Twilio) ni les clés WhatsApp de Meta ne sont en place sur ce serveur. Avec `PHONE_OTP_ENABLED=1` le message ne paraît plus : le code part par le fournisseur de Supabase, et le formulaire dit le canal réellement emprunté. Sans l'un ni l'autre, les clés WhatsApp ne sont pas sur Vercel (constat du 19 septembre 2026 : ni `WHATSAPP_*`, ni `RESEND_API_KEY`). Tant qu'elles manquent, le formulaire laisse partir l'intention avec la mention « WhatsApp non prouvé » et le desk confirme le numéro par téléphone ; le code de démonstration ne s'affiche jamais en production. Renseigner `WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID` (Meta Cloud API) et `RESEND_API_KEY` + `EMAIL_FROM`, puis Redeploy. |
| Twilio : « Primary compliance profile is not approved » (erreur 20003) | Le compte Twilio n'a pas encore passé la conformité (Trust Hub) : il refuse d'émettre, et le code ne part pas. C'est un dossier à remplir chez Twilio, pas un réglage de l'application : Twilio Console › Trust Hub › *Primary Customer Profile*, puis le bundle réglementaire du pays destinataire (le Kenya, par exemple, exige un Sender ID déclaré). Tant que le dossier n'est pas approuvé, l'application dit au client que le code n'est pas disponible et que le desk confirmera le numéro ; le message de Twilio reste dans les journaux Vercel, jamais à l'écran du client. |
| Le code du téléphone n'arrive pas (Supabase) | Le code passe par Supabase › Auth › Phone, donc par Twilio : vérifier le solde et les journaux Twilio, puis Supabase › Logs › Auth. Un numéro déjà porté par un autre compte est refusé par Supabase, en toutes lettres dans le formulaire. Le canal par défaut d'un numéro nouveau est le SMS (Supabase ne laisse pas choisir sur un changement de numéro) ; `PHONE_OTP_CHANNEL` ne vaut que pour la connexion et pour un numéro déjà prouvé. |
| Le code WhatsApp n'arrive pas (Meta) | Santé › canaux (WhatsApp configuré ? jeton expiré ?), Messages › la conversation (le numéro a-t-il dit STOP ?). En attendant : répondre au client depuis Messages avec « Répondre avec la ligne » : le lien signé vaut preuve du numéro. |
| Le même message se réécrit tous les jours | Il a sa place dans les messages préparés : desk › un ordre › « Écrire au client ». Les textes se relisent dans Référentiel › Modèles › Messages préparés (version, relecture par un autre membre du desk, historique), exactement comme un passage de document. Le message s'ouvre dans WhatsApp ou dans le courrier de l'opérateur : l'application ne l'envoie jamais seule, elle note seulement qu'il a été ouvert, dans le fil de l'ordre. |
| Chaîne d'audit « rompue » | Ne rien modifier ; prévenir le responsable ; comparer avec Supabase › Table `audit` (la table refuse toute modification, un écart signale une intervention directe en base). |
| Publication bloquée « hors fenêtre » | Normal : un responsable décide dans Approbations. Si la fenêtre est trop étroite, le responsable l'élargit (Approbations › Fenêtre déléguée). |
| Vercel : build rouge après un push | Déploiement précédent reste en ligne. Ouvrir le log de build, corriger, repousser ; ou *Promote* le précédent. |
| Supabase en pause (plan gratuit, inactivité) | Dashboard › *Restore project*. Les crons quotidiens l'empêchent normalement. |
| « Le site refuse les robots » sur Lire la page | Le site (COSUMAF…) bloque les robots : remplissez titre et date à la main ; le lien reste considéré vivant par la vérification de nuit. |
| Lien mort signalé sur Santé | desk › Actualités › la publication : corriger le lien ou la retirer. Les liens reçus non triés depuis plus de 7 jours y apparaissent aussi. |
| Un texte de la documentation ou du guide est faux | Il se corrige dans le code (`src/data/docs`, `src/data/desk-guide.ts`), pas dans une base ; mettre à jour la date « vérifié le » et régénérer les captures si l'écran a changé. |

## 8. Adresse publique et mise en ligne

**Adresse de production : https://guichet.purposecapital.africa** (depuis le 21 septembre 2026). Le domaine est déclaré sur Vercel (projet guichet, environnement Production) et pointé par un CNAME `guichet` → `8200882685b486f7.vercel-dns-017.com` dans la zone Netlify DNS de `purposecapital.africa` ; le certificat est émis par Vercel. `NEXT_PUBLIC_APP_URL` vaut cette adresse (liens envoyés, clés d'accès liées au domaine) ; Supabase › Authentication › URL Configuration a le Site URL et deux redirections (`/auth/callback`, `/**`) sur ce domaine. L'ancienne adresse `guichet-seven.vercel.app` répond toujours (même déploiement) : les clés d'accès enregistrées dessus ne valent que là, à ré-enregistrer une fois sur le vrai domaine. Reste à faire avant l'ouverture : Supabase Pro (sauvegardes quotidiennes, pas de mise en pause), Vercel Pro (usage commercial, sept tâches planifiées), clés VAPID, coordonnées bancaires de règlement, retrait des données de démonstration, liste des e-mails du desk.

**Le desk a son adresse : https://desk.purposecapital.africa** (21 septembre 2026). Même application, même déploiement Vercel, même Supabase : la variable `NEXT_PUBLIC_DESK_HOST` (= `desk.purposecapital.africa`) fait que le proxy (`src/proxy.ts`, règles dans `src/lib/hosts.ts`) garde `/desk` sur cet hôte et renvoie tout le reste vers le site client, même chemin ; « / » du desk mène à `/desk` ; un client qui atteint le desk revient sur le site client. Le site client ne montre aucun bouton desk, même à un membre du desk (en-tête, ⋮, compte, onglets) ; sur l'hôte du desk, pas de barre d'onglets client. La session est propre à chaque hôte : un membre du desk se connecte sur desk.purposecapital.africa (code par e-mail, le lien de l'e-mail revient sur l'hôte qui l'a demandé), puis MFA. DNS : CNAME `desk` → le même enregistrement Vercel que `guichet` (zone Netlify) ; Supabase › URL Configuration a `https://desk.purposecapital.africa/**`. Sans la variable (dev, prévisualisations), un seul hôte sert tout, comme avant. **L'encre en bas d'écran** (bande sombre sur la barre d'onglets, par moments, sur téléphone) venait des feuilles « ··· » de chaque carte et des feuilles du ⋮, montées en permanence avec leur voile navy à opacité zéro : cinquante calques fixes plein écran que Safari repeignait parfois ; une feuille n'est plus dans la page que le temps de son ouverture (`useSheetPresence`). Diagnostic : `?debug=bar` liste ce qui recouvre la barre.

**La fiche, l'émetteur, l'intention** (21 septembre 2026). La fiche se lit en quatre volets, Essentiel · Chiffres · Documents · Émetteur ; le volet « Risques » n'existe plus (une ligne renvoie à la leçon « Les quatre risques »). Le volet Émetteur vient du **registre des émetteurs** (`src/data/issuer-registry.ts`) : une entité par emprunteur avec ses graphies (le bulletin, les saisies du desk et les fiches BVMAC n'écrivent pas un émetteur de la même façon : « État du Gabon », « Trésor public de la République gabonaise », « EOG »), sa famille, sa zone (un pays, ou CEMAC pour la BDEAC et la BEAC), une ou deux phrases avec leur source. Règles : rien n'est inventé, seule l'information qu'une source donne s'affiche, ce qui manque est silencieux (pas de mention « non communiqué » côté client) ; les libellés et chiffres des lignes restent ceux du bulletin ; la liste groupe et trie sur le nom du registre ; une ligne dont l'émetteur est inconnu du registre garde sa graphie. Pour ajouter un émetteur : une entrée dans `STATES` / `REGIONAL`, ou une fiche dans `companies.ts` / `issuers.ts` (reprises automatiquement). L'intention a sa page, `/offres/<id>/intention` (`intent`, `qty`, `de` en paramètres) : sur téléphone, la barre du bas, la traction de la carte et le lien WhatsApp du desk y mènent ; sur grand écran, la colonne de droite de la fiche reste.

**Aujourd'hui, Dépôt, documentation** (21 septembre 2026). Le carnet s'ouvre sur « Aujourd'hui » (`src/app/desk/today`) : le bulletin attendu (séance du jour dès 19 h 30 Yaoundé, sinon la veille de bourse) contre le dernier lu, les lignes à rafraîchir (cotées sans le dernier cours, résultats à saisir), la sélection (et ses lignes clôturées), les intentions non traitées et l'âge de la plus ancienne, les sources à valider, la santé ; sous la tuile du bulletin, quand il manque ou a échoué, « relancer » et « déposer le PDF ». **Dépôt** (desk › Pilotage › Dépôt, `/desk/depot`) : tous les bulletins lus avec le PDF conservé (`/desk/depot/boc/<date>` lit le bucket « sources », clé `boc/BOC-AAAAMMJJ.pdf`) et le lien BVMAC, les sources reçues avec leur fichier, le compte des documents émis, les modèles, les références (fiches et comptes des émetteurs, sources des modalités). **Documentation** : deux pages nouvelles dans desk › Documentation, « Relation client et communication » (parcours, canaux et règles, délais, mots, situations, journée) et « Sources, stockage et dépôt » (chaque information → source → lieu → conservation → accès ; règles des lieux de stockage ; le dépôt ; le bulletin de la BVMAC à la fiche), avec six schémas dessinés (`src/components/docs/DocDiagrams.tsx`, bloc `diagram`) : sources et stockage, journée, parcours du client, canaux, chaîne documentaire, hébergement ; « Comment fonctionne Guichet » et « Plateformes » en reçoivent aussi. La version téléphone du desk est écartée pour l'instant (le desk se lit sur ordinateur).

**Registre des modèles** (21 septembre 2026, desk › Référentiel › Modèles, `/desk/referentiel/modeles`). Ce que disent les documents que le Guichet produit se règle depuis le desk, passage par passage : pour chaque modèle (bulletin d'ordre, appel de fonds, ordre de cession, avis de résultat, avis d'opéré, relevé, attestation, convention article par article), la page dit quand la vie d'une intention le produit, montre le texte en vigueur (français et anglais, avec les champs remplis sur des données d'exemple), et ouvre l'éditeur : les champs comme des pastilles qui s'insèrent (`{societe}`, `{prix}`…, ceux marqués * doivent rester), une note pour l'historique, « Aperçu PDF avec ce texte » sur des données de démonstration. Chaque passage porte sa règle d'entrée en vigueur : **libre** (lignes de signature, notes de pied) en vigueur dès l'enregistrement ; **relu** (provenance des fonds, portée d'une attestation…) en vigueur quand un autre membre du desk que l'auteur l'a relu ; **réglementaire** (demande d'ordre, irrévocabilité, attestation du cédant, règlement-livraison, articles de la convention) en vigueur quand un responsable l'a approuvé. Une version en attente s'affiche en orange avec son bouton (« Relu : mettre en vigueur », « Approuver et mettre en vigueur ») ; l'historique garde chaque version remplacée avec « Revenir à cette version » ; un responsable peut « Rétablir le texte d'origine ». La mise en page, les chiffres, les références et les blocs de données restent dans le code (`src/lib/documents/pdf/*`) ; un modèle nouveau reste une affaire de développement. **Ce que garde un document** : chaque PDF généré enregistre la version de chaque passage qu'il portait (`documents.template_versions`), un document déjà émis ne change donc jamais. Le journal reçoit chaque enregistrement, mise en vigueur et retour. Code : catalogue et règles dans `src/lib/documents/passages-catalog.ts` (sans dépendance serveur, lu par l'éditeur), résolution contre la base dans `src/lib/documents/passages.ts` (`resolvePassages`, tolère l'absence de la table), page et actions dans `src/app/desk/referentiel/modeles`, aperçu `/desk/referentiel/modeles/preview?type=…` (desk seulement, jamais stocké). Base : migration `0030_template_texts.sql` (table `template_texts`, colonne `documents.template_versions`), appliquée le 21 septembre ; sans la table, le desk lirait le texte d'origine et l'enregistrement échouerait proprement. Le Dépôt renvoie au registre pour les modèles. **Trois kinds et quatre documents nouveaux** (21 septembre 2026, soir). Le registre liste tous les modèles en trois groupes dans l'ordre de la vie d'une relation : **Signés par le client** (convention, mandat, bulletin d'ordre, ordre de cession, réclamation, ordre de transfert / clôture ; règle réglementaire), **Envoyés au client** (appel de fonds, avis de résultat, de non-allocation, d'opéré, de coupon / remboursement, relevé, attestation ; règle relu), **Transmis aux contreparties** (dossier SVT, bordereaux ; libre, jamais au client). L'avis de non-allocation a désormais son passage. **Les quatre documents nouveaux et leurs déclencheurs** : (1) **Mandat de gestion des ordres** : desk › Dossiers › bloc « Actes et avis » › « Établir un mandat » (mandataire, pièce, lien, étendue cochée : ordres · avis · limité aux fonds ; date de fin), PDF PC-MAN envoyé au client par son canal prouvé, « Signé des deux parties » marque le document signé, « Révoquer » le clôt ; les fonds ne sortent jamais vers le mandataire. (2) **Avis de coupon / remboursement** : chaque position garde ses flux payés (`Position.paid`) ; desk › Dossiers › « Émettre un avis » (flux, payé le, canal, note) ou la tuile **Aujourd'hui › « Coupons et remboursements à aviser »** avec « Émettre les avis » en lot ; un avis par flux (`documents.flow_key`, unique), envoyé seulement par un canal prouvé, sinon gardé au dossier. (3) **Réclamation** : côté client, Mon espace › « Déposer une réclamation » (`/moi/reclamation`) : l'opération proposée depuis l'historique, les faits, la demande, signature par code WhatsApp si le numéro est prouvé, sinon la session e-mail prouvée ; PDF PC-REC avec les délais (accusé sous 2 jours ouvrés, réponse sous 30 jours, recours COSUMAF), copie dans Mes documents, un message dans desk › Messages avec l'horloge ; côté desk, « Enregistrer une réclamation » pour celles reçues par appel, WhatsApp, e-mail ou courrier. (4) **Ordre de transfert / clôture** : desk › Dossiers › « Transférer ou clôturer » (toutes les positions et clôture, certaines positions, ou clôture sans position ; établissement et compte de destination), PDF PC-TRF envoyé pour signature ; « Ordre signé par le client » passe le dossier **en clôture** (statut `en_cloture`, les nouvelles intentions sont refusées), « Transfert confirmé par le dépositaire » le passe **clos** et joint le relevé final ; « Abandonner l'ordre » rend le dossier actif. Base : migration 0031 puis **0032** (valeurs d'enum `document_type` mandat · coupon · reclamation · transfert, `kyc_status` en_cloture · clos, `documents.flow_key`, `client_files.acts` jsonb). **La carte des documents et les parcours, vivants** (22 septembre 2026). Une seule table dit qui fait quoi de chaque document (`DOC_ROLES` dans `src/lib/documents/registry.ts` : moment, qui prépare, qui signe, qui reçoit, où il naît, où le trouver, l'horloge) et un seul fichier raconte les six parcours (`src/data/flows.ts`). Deux blocs de documentation les lisent (`docmap`, `flows`, composant `src/components/docs/DocMap.tsx`) : la carte (trois kinds × six moments, chaque document s'ouvre en feuille : rôles, quand, règle du texte, versions en attente, « Où il naît », « Où le trouver » = Documents filtré `?type=`, « Le modèle ») et les parcours en onglets de cartes d'étapes colorées par acteur, l'horloge du pas quand il y en a une. Le **Dépôt** porte la même carte avec le nombre émis par modèle et un point orange pour les versions de texte en attente (`docStats`). **Dessin de la carte** (22 septembre 2026, soir) : une frise des six moments en tête (points reliés, une ligne sous chacun : « dossier accepté », « intention confirmée »…), les trois genres en étiquettes verticales de 34 px, des tuiles dans les couleurs de l'application (or, bleu, gris) portées par une ombre, le nombre émis sous le titre ; six colonnes égales, rien ne défile à 760 px ; sur téléphone la même carte tourne (moments en lignes, genres en colonnes). Le mot « nouveau » a disparu des tuiles. Desk › Documentation : le sommaire de gauche ne défile plus dans sa colonne. **Un seul écart sous le bandeau Pilotage** (22 septembre 2026) : 42 px, posé une fois sur la barre (`DeskNav.module.css`), aucune page n'ajoute le sien ; sommaire, article et volets se posent sur la même ligne. **Modèles consultables** (22 septembre 2026). Tout modèle du registre s'ouvre en aperçu PDF sur des données de démonstration, y compris le dossier d'ouverture SVT et les deux bordereaux (« Aperçu PDF · bordereau SVT », « · bordereau OPCVM », `?variante=opcvm`) : leurs phrases de règlement-livraison, d'instruction d'exécution et de demande d'ouverture sont devenues des passages (règle libre), les bordereaux et dossiers émis gardent la version portée. Sous le titre de chaque modèle, « Dernier émis » (numéro, date, client, PDF, lien vers Documents filtré) ou la phrase qui dit qu'aucun n'a encore été émis. Un modèle sans passage se lit par l'aperçu ; une modification de texte se demande à l'équipe technique. **Autres éditions** en bas du registre : la fiche PDF d'une ligne, le rapport société et le rapport d'activité, produits à la demande, non numérotés, s'ouvrent en aperçu sur une ligne, une société ou la période courante. **Contrôle sanctions sans fournisseur** (22 septembre 2026) : quand aucune clé OpenSanctions n'est configurée, le bouton « Lancer le pré-contrôle automatique » ne s'affiche plus ; à sa place, les listes publiques à consulter (OpenSanctions recherche gratuite avec le nom prérempli, liste consolidée ONU, carte des sanctions UE, OFAC) et les noms du dossier à vérifier, puis l'attestation. L'abonnement OpenSanctions (une centaine d'euros par mois) reste une décision à prendre ; avec la clé, le bouton revient. **Statuts côté client, documents des lignes, calendrier** (22 septembre 2026). Le client lit quatre mots sur une ligne : « Cotée » ou « Souscription ouverte » (se traite à toute séance), « À venir », « Ouverte » (les dernières heures se lisent au compte à rebours), « Clôturée » pour toute fenêtre passée (fermée, résultats, en vie, échue : le détail reste sur la fiche et au desk, `statusLabel(o, s, fine)`). Le filtre Statut de Titres n'a plus que ces familles plus « Sélection du desk ». Une ligne primaire réglée ou échue et cotée depuis à la BVMAC ne figure qu'une fois sur Titres, par sa ligne du bulletin (même ISIN). **Documents d'une ligne** : plus aucun titre sans fichier ; desk › la ligne › « Documents de la ligne » : « Joindre le document » (PDF, PNG, JPEG, 15 Mo, bucket « sources », `lignes/<id>/…`, servi à `/offres/<id>/doc/<n>`), « Retirer » ; les intitulés de démonstration des lignes RCA (communiqué 0762/MCFB, note, teaser) ont été retirés de la base. **Comparer** : le calendrier des flux prend toute la largeur et se dessine à sa propre largeur. **Documentation** : « Les documents, de l'intention à la clôture » (`/desk/docs/documents`) porte la carte des documents (schéma) et les parcours opération par opération. **L'indice BVMAC All Share** (22 septembre 2026). Le bulletin est lu depuis toujours avec le bloc de l'indice (`market_bulletins.index_value`, `index_variation_pct`) et la page des capitalisations (sur chaque cotation d'action : `market_cap_total`, `market_cap_float`, `shares_*`, `last_dividend`, `liquidity_3m_pct`). Désormais il se montre : le **pouls** en tête de Titres et d'Actualités (`IndexPulse`, niveau, séance, un mois, depuis le 1er janvier, douze mois, soixante séances, étiquette « indice de prix », lien vers la leçon) ; sur la page d'une **société**, cours et indice en base 100 sur la période choisie avec la phrase de lecture, le poids dans l'indice (capital global et flottant) et le dernier dividende (`IndexVsShare`) ; dans **Comparer**, deux actions prennent l'indice sur douze mois comme repère à la place du BTA, plus une ligne « Dans l'indice BVMAC » ; **Santé** gagne « Indice BVMAC et cours d'actions » (une variation publiée sans cours d'action changé dans la lecture, ou l'inverse, passe en orange ; `indexCheck`) ; le **Guide** gagne la leçon « L'indice de la BVMAC : ce qu'il dit, ce qu'il ne dit pas » (parcours Instruments, widget `indice` : niveau vivant, poids, curseur « une valeur bouge ») ; la **documentation** desk gagne « L'indice BVMAC All Share » (`/desk/docs/indice`). Règle : Guichet montre et explique l'indice, n'en construit pas et ne mesure jamais un client contre lui. La note méthodologique de la BVMAC est à obtenir (courrier prêt : `docs/courrier-bvmac-indice.md`) ; d'ici là les poids s'affichent dans les deux lectures, marquées « à confirmer ». Calculs : `src/lib/market/index.ts`. **La page de l'indice** (22 septembre 2026, `/indice`, publique) : le niveau et ses écarts (séance, un mois, depuis le 1er janvier, douze mois, plus haut et plus bas douze mois, séances lues dont sans mouvement) ; le graphique de toutes les séances lues (`IndexChart`, périodes 1 mois · 3 mois · depuis le 1er janvier · 12 mois · tout, réticule au survol avec date, niveau, variation, écart depuis le début de la période et les valeurs qui ont bougé, un point puis deux points épinglés au toucher ou par deux dates pour lire l'écart, base 100, une action cotée superposée en base 100, points colorés sur les séances avec mouvement, une semaine sans bulletin = une coupure de la ligne) ; le tableau des seules séances où l'indice a bougé avec la société qui a traité ; la composition sur les deux pondérations avec l'explication capital global / flottant coté ; comment le lire ; la méthode à confirmer. Liens depuis le pouls (« voir l'indice »), la leçon, le bloc cours et indice d'une société, Comparer. **Sept vues et le panneau Données** (22 septembre 2026, soir) : le graphique passe sous un sélecteur de vues, mêmes périodes, réticule et épingles partout où ils ont un sens : Niveau (le réticule dit aussi titres, montant et transactions de la séance), Niveau + volumes (barres du montant, des titres ou des transactions, toutes sociétés ou une seule, dorées quand l'indice a bougé), Contributions (le mouvement de la période décomposé par société : poids en début de période × variation du cours, sur le capital global ou le flottant, la somme, l'écart avec la variation publiée ; deux épingles fixent la période), Calendrier (une case par jour ouvré, hausse, baisse, plate, pointillé quand aucun bulletin n'est lu ; toucher une séance l'épingle), Sociétés en base 100 (sept vignettes contre l'indice), Capitalisation (la cote en francs, capital global et flottant empilés), Flottant (l'indice publié contre une lecture en flottant des mêmes cours, base 100, marquée « lecture Guichet, non publiée » ; la rotation du flottant par société = montant échangé ÷ flottant coté). Les capitalisations par séance viennent du dernier nombre de titres lu × le cours de clôture (`src/lib/market/index-data.ts`, `IndexChart`). **Panneau Données** : dernier bulletin et sa lecture, historique, jours ouvrés sans bulletin (jours fériés compris), séances avec mouvement, montant échangé douze mois et part de la première valeur, capitalisation, flottant et rotation, méthode, source, téléchargement de la série (`/indice/serie.csv`). **Rotation du flottant** partout où l'on choisit une action : sous le bloc « Cours et indice » d'une société (pourcentage douze mois, étiquette liquidité faible / moyenne / vive selon 5 % et 20 %, montant, transactions, flottant, une phrase de conduite) et dans la ligne « Dans l'indice BVMAC » de Comparer (`floatRotation`, `rotationWording` dans `src/lib/market/index.ts`). Sur le Calendrier, la bulle d'une séance est sombre : jour en toutes lettres, niveau et variation, séance précédente, valeurs qui ont bougé, titres, montant et transactions, et pour un jour sans bulletin la raison possible. **Bulles et infobulles, une règle** (22 septembre 2026) : une bulle est l'inverse du fond, dans tous les thèmes et toutes les palettes : jetons `--tip-bg`, `--tip-ink`, `--tip-ink-2`, `--tip-ink-3`, `--tip-line`, `--tip-accent`, `--tip-up`, `--tip-down`, `--tip-shadow` dans `globals.css` (clair et dim : bulle navy, texte blanc ; sombre : bulle claire, texte navy ; contrastes vérifiés au-dessus de 4,5:1 pour les couleurs, 8:1 pour le texte). Les infobulles des cours (`Charts`, `QuoteHistory`), du graphique et du calendrier de l'indice les utilisent ; elles sont fixées à la fenêtre, tenues dans ses bords, basculent au-dessus près du bas. Sur écran tactile, un premier toucher montre la lecture, un second sur le même point épingle. Les tracés de graphiques prennent `--chart-out` (navy en clair, bleu clair en sombre), plus jamais `--navy` : la ligne de l'indice était invisible en sombre système. Option « Tracé » du graphique de l'indice : ligne, ligne et points, points. **La note mensuelle sur l'indice** (22 septembre 2026, nuit) : un document de plus (`note_indice`, préfixe IDX, genre « envoyé au client »), écrit seul à partir des bulletins déjà lus. `src/lib/market/index-note.ts` calcule tout ce que la note dit (niveau et performance du mois, séances lues et séances avec mouvement, plus haut et plus bas, négoce, contribution de chaque société, rotation du flottant, séances dont la variation publiée ne se reconstitue pas) et rédige les trois phrases de tête ; `src/lib/documents/pdf/index-templates.tsx` la met en page ; le passage « portée » est modifiable (règle relu). Desk › Marché › Indice (`/desk/indice`) montre le mois, ses chiffres, ce qui a fait le mouvement, les séances à éclaircir, l'aperçu PDF et le bouton « Publier » ; la publication garde le PDF dans Documents avec son numéro (PC-IDX-AAAAMM) et les versions de passage. Le robot prépare la note du mois écoulé le 3 du mois suivant (`/api/cron/note-indice`, cron Vercel « 40 6 3 * * ») : il produit et garde, il n'envoie rien ; une personne relit et envoie. Base : migration `0033_note_indice.sql` (valeur d'enum). **Quatre lectures et une note par vue** (22 septembre 2026, nuit) : la Capitalisation se lit « en francs », « échelle log » (mêmes francs, axe logarithmique : une même hausse en pourcentage occupe la même hauteur), « croissance, base 100 » et « part de la cote » (la part de chaque société dans la capitalisation, séance après séance). Chaque vue du graphique de l'indice porte un bouton « Comment lire ce graphique », replié par défaut, qui explique en un paragraphe ce que l'œil regarde (`HowTo`, `CAP_HOW`). **Capitalisation et flottant, lisibles** (22 septembre 2026, soir) : la Capitalisation se lit « en francs » (la cote entière, capital global et flottant, plus une barre « Aujourd'hui, qui pèse quoi » qui donne la part de chaque société) ou « croissance, base 100 » (les sept sociétés à 100 au début de la période, une couleur chacune, la plus forte et la plus faible nommées) : l'empilement, illisible, a disparu. Sur le Flottant, choisir une société trace son cours en base 100 contre l'indice publié et la lecture en flottant, et sa barre de rotation est simplement mise en avant, sans estomper les autres. **La société dans trois vues** (22 septembre 2026) : un sélecteur « Société » sur le Calendrier (les cases prennent la variation du cours de cette valeur, hachurées quand elle s'échange sans changer de cours, la bulle lit son cours, son négoce puis l'indice), sur la Capitalisation (par défaut les sept sociétés empilées de la plus lourde à la plus légère, étiquettes de part en bout de bande ; une société choisie se montre seule, capital global et flottant) et sur le Flottant (la barre de rotation de la société en évidence, sa part du flottant de la cote, du capital global, et le flottant en part de son capital). **Suivi et intervalle sur toutes les courbes de l'indice** (22 septembre 2026) : `useTracker` et `TrackMarks` dans `IndexChart` donnent à la Capitalisation et au Flottant le réticule, la bulle fixée à la fenêtre et les deux épingles ; les épingles sont communes aux vues (posées sur Niveau, lues sur Contributions, Capitalisation et Flottant) ; sur le Flottant, l'intervalle épinglé devient la fenêtre de la rotation. **Une seule boîte pour choisir** (22 septembre 2026) : le `Select` de l'application (`src/components/ui/Select.tsx`, 45 emplois, plus aucun `<select>` natif) devient un champ de recherche quand il s'ouvre : la même boîte se tape, la liste entière s'affiche sous elle tant que rien n'est tapé, puis se resserre en tapant (accents ignorés, libellé et indice cherchés, les titres de groupe restent quand un de leurs éléments correspond) ; flèches, Entrée, Échap ; champ de formulaire par l'input caché. Sur téléphone la liste s'ouvre en feuille basse avec le champ en tête et une croix. Appliqué partout, quelle que soit la taille de la liste. **Le suivi sur tous les graphiques** (22 septembre 2026) : `src/components/charts/tracker.tsx` (`useTracker`, `TrackMarks`, `TrackTip`, `RangeRead`, `togglePin`) porte le réticule, la bulle fixée à la fenêtre sur les jetons `--tip-*`, le toucher qui lit puis épingle, et l'intervalle entre deux épingles. Adopté par le cours de clôture d'une société (écart de cours, séances, titres et montant échangés entre deux dates), la VL d'un fonds (deux touchers recadrent la période de toute la page : `du`/`au` dans l'adresse, les modes et les chiffres suivent), les deux courbes de Comparer (lecture des deux lignes entre deux dates) et le graphique des flux d'une obligation, fiche et simulateur (ce qui revient entre deux flux, le cumul, la part de la mise). **Le téléphone, cinq points** (22 septembre 2026) : (1) la liste d'un `Select` s'ouvre ancrée sous la boîte, au-dessus quand la place manque, mesurée contre la fenêtre visible (le clavier ne la cache pas), tenue dans l'écran ; plus de feuille basse ; (2) le clavier ne vient que sur demande : une liste s'ouvre en liste, la loupe en tête (ou une frappe au clavier) fait apparaître le champ ; sur ordinateur le champ est focalisé pour les listes de plus de douze entrées seulement ; (3) les repères de dates du graphique de l'indice suivent la largeur (deux sur téléphone, format court) ; (4) « Tracé » : ligne, ligne et mouvements (les marqueurs colorés), points ; les points creux par séance ont disparu ; (5) au doigt, glisser fait défiler la lecture (`touch-action: pan-y`, capture du pointeur), lever le doigt la laisse, toucher le point lu l'épingle, un appui long puis glisser dessine l'intervalle en direct ; le calendrier se lit aussi en glissant. Tout vit dans `Select` et `charts/tracker.tsx`. Le curseur en croix et la bulle hors de son chemin (jamais sous le pointeur, `pointer-events: none`) viennent de la classe partagée : les quatre autres lectures d'un fonds (rendement annualisé, million placé, variations, repli) passent aussi au tracker et posent la période à deux clics comme la VL et l'indice. Une lecture laissée par le doigt se ferme au toucher ou au clic suivant hors du graphique, au défilement, au redimensionnement et sur Échap (`useTracker`, et la bulle du calendrier de l'indice). **Tableau des séances** : avec mouvement par défaut, « toutes les séances » avec titres, montant et transactions, filtre par société, cinquante par page (`?toutes=1&societe=&page=`). **Leçon indice** (même jour) : avant → après dans l'en-tête, barres qui se recalculent, jauge à deux aiguilles (la valeur, l'indice), pondérations expliquées sous le sélecteur. Le scénario garde un déplacement par ligne (choisir une ligne, bouger le curseur, choisir une autre) : chaque ligne déplacée porte une pastille avec son déplacement et un « × » pour le retirer, l'indice réagit à la somme pondérée, la phrase donne la contribution de chaque ligne, « Revenir aux poids réels » efface tout ; changer de pondération recalcule sans effacer. Les dimensions du bloc ne bougent pas pendant la simulation. Code : `src/lib/documents/pdf/acts-templates.tsx`, générateurs dans `generate.ts`, actions `src/app/desk/clients/acts-actions.ts`, panneau `ClientActs.tsx`, page client `src/app/moi/reclamation`, `notifyClientDocument` (dispatch.ts) pour un document sans intention. **La note trimestrielle, publique** (22 septembre 2026, nuit). L'activité de la cote est lente : la note qui sort est trimestrielle, la mensuelle reste au desk pour le contrôle. `src/lib/market/index-quarter.ts` (`quarters`, `quarterNote`) calcule le trimestre à partir de ses seules séances, donc une note close ne bouge plus : niveau et performance, fins de mois, plus haut et plus bas, séances lues et séances avec mouvement, négoce, et pour chaque société son activité, son pays, son cours, son dividende et son rendement, son poids, sa part de flottant, sa rotation, sa variation ; plus les répartitions par secteur et par pays, et `methodOpen` qui réduit toute la réconciliation à une ligne (le détail reste au desk). La page publique `/indice/note/<trimestre>` (`src/app/indice/note/[trimestre]`) se lit sans compte, en quatre sections numérotées : ce que le trimestre a fait (barres des fins de mois, tableau des séances qui ont bougé), les sept sociétés derrière le chiffre (tableau complet, répartitions), ce qui s'est échangé (rotation), ce que l'indice mesure et ne mesure pas. « Télécharger en PDF » pointe `/indice/note/<trimestre>/pdf` (`quarter-templates.tsx`, même contenu en mise en page maison). Le panneau « Les notes de marché » de `/indice` liste les six derniers trimestres. Desk › Indice porte désormais les deux notes : la trimestrielle en tête (pastilles avec ✓, phrase d'attaque, lien vers la page publique et vers le PDF, bouton « Publier le … »), la mensuelle en dessous. Publier fige le PDF dans Documents avec son numéro (PC-IDX-AAAATQ) et les versions de passage ; la page, elle, se calcule à la demande et un trimestre en cours est déjà lisible. Le robot prépare le trimestre clos le 5 janvier, avril, juillet et octobre (`/api/cron/note-trimestre`, cron « 0 7 5 1,4,7,10 * »), idempotent, n'envoie rien. **Documentation de maintenance** : `docs/notes-de-marche.md` (chaîne des chiffres, calendrier, relecture, publication, destinations, pannes, carte des fichiers) et sa version dans l'application, Desk › Documentation › « Les notes de marché » (`/desk/docs/publications`, `src/data/docs/publications.ts`). Règle de partage : le lien de la page, pas le fichier ; les séances à éclaircir ne sortent jamais du desk. **Deux références par document** (22 septembre 2026, nuit). Une référence qui compte donne le compte : `PC-AF-2026-0002` disait au client qu'il tenait le deuxième appel de fonds de l'année, et deux documents reçus à quelques semaines d'écart donnaient le rythme. Chaque document porte désormais deux références (`src/lib/documents/numbering.ts`, `nextNumbers`, `registerOf`, `isSequentialRef`). **La référence imprimée** (`documents.number`), celle que le client cite : pour un document qu'un client détient elle devient opaque, `PC-BUL-260922-K7Q4`, le modèle, le jour d'émission et quatre caractères tirés au sort dans un alphabet sans I, L, O, U, zéro ni un, qui s'épelle au téléphone. **L'entrée du registre** (`documents.register_no`), `PC-BUL-2026-0018` : une suite ininterrompue par modèle et par année, lue au desk et par un contrôleur, jamais portée sur l'exemplaire d'un client. Deux exceptions voulues : un document transmis à une contrepartie (bordereau, dossier SVT ; genre « interne ») garde sa suite comme référence, la BVMAC et le Trésor lisant un registre ; une note sur l'indice garde la référence de sa période (`PC-IDX-2026T2`), ce qui est aussi ce qui rend sa préparation idempotente. Le registre se calcule de la plus haute entrée déjà inscrite, plus jamais du nombre de lignes : un document retiré ne rend plus son numéro au suivant, et deux documents émis dans la même seconde ne se télescopent plus. Une référence déjà entre les mains d'un client ne change jamais : les documents émis avant gardent la leur, qui sert d'entrée de registre (`registerOf`). Desk › Documents gagne une boîte `?q=` qui accepte l'une ou l'autre référence (plus le client et le titre) et montre l'entrée de registre sous la référence imprimée. Le journal porte les deux. Documentation : Desk › Documentation › Les documents › « Les deux références d'un document ». Base : migration `0034_document_register.sql` (colonne + index unique partiel), appliquée et vérifiée le 22 septembre. Test : `src/test/numbering.test.ts`. **Le balayage des identifiants visibles du client** (22 septembre 2026, nuit). La référence d'une intention portait la même fuite, en pire : le client la voit en premier et partout (accusé de réception, bulletin d'ordre, Mon espace, motif de son virement) et son rang venait d'une séquence Postgres jamais remise à zéro, donc `PF-0914-018` disait « dix-huitième ordre de la maison ». `makeRef(type, now)` (`src/lib/data/repository.ts`) rend désormais `PF-0914-K7Q4` : l'opération, le jour, quatre caractères du même alphabet sans I, L, O, U, zéro ni un ; `makeOrderNo(seq)` garde le rang dans le **journal des ordres** (`intents.register_no`, `PC-ORD-000018`), lu sur la fiche d'une intention au desk et en première colonne du journal exporté (le CSV ouvre sur « N° journal », puis « Référence client »). Une collision de référence, très improbable, est retirée au tirage suivant ; la colonne manquante ne bloque pas la prise d'ordre (même dégradation gracieuse que les autres migrations). Le reste du balayage ne compte rien et ne change pas : l'adresse d'une ligne est son nom (`rca-ota-c-2028`), `/offres/<id>/doc/<n>` numérote dans la ligne seule, dossiers, documents, comptes et positions sont des identifiants tirés au sort, le numéro de sous-compte est attribué par le SVT, et la seule autre séquence de la base (`audit.id`, bigserial) ne sort jamais du desk. Base : migration `0035_intent_register.sql`, appliquée et vérifiée le 22 septembre. Tests : `src/test/numbering.test.ts`. **La note trimestrielle, en papier complet** (23 septembre 2026). La première version portait le contenu mais pas la mise en page : elle a été reprise pour ressembler à la maquette approuvée. La page (`src/app/indice/note/[trimestre]/page.tsx`) passe à sept sections et deux colonnes : l'article, et une **marge technique collante** (séances lues, dont sans mouvement, première et dernière séance, plus haut, plus bas, repli maximal, variation moyenne, écart-type, variabilité annualisée, composition, capitalisation). **Quatre dessins** (`Figures.tsx`, SVG rendu au serveur, couleurs prises aux jetons du thème, lisibles en clair comme en sombre) : la courbe de l'indice séance par séance avec un point coloré sur chaque séance qui a bougé et la séance la plus forte annotée ; les trois fins de mois en barres signées ; le poids de chaque société en barres appariées capital global / flottant coté, avec légende ; l'apport de chaque société en points d'indice. **Trois sections d'analyse nouvelles**, en langue de client : « Ce que cet indice fait bien », « Le point à connaître : la concentration », « Ce que l'indice dit, et ne dit pas, de l'économie de la région » (avec « ce qui ferait de cet indice un vrai baromètre »), plus l'encadré **Méthode** en pied d'article. Le détail de la réconciliation reste au desk : la note n'en dit toujours qu'une ligne. **Le PDF suit** (`quarter-templates.tsx`) : mêmes sept sections, la courbe et les barres de poids en SVG react-pdf, la fiche technique en KV et l'encadré Méthode ; sept pages. Données ajoutées à `index-quarter.ts` : `points` (la série du trimestre), `peak`, `stats` (moyenne, écart-type, deux variabilités annualisées, repli maximal et ses dates), `financePct`, `holderPct`. **Trois corrections que la vérification a trouvées** : la phrase sur la variabilité affirmait un écart entre les deux mesures alors qu'elles se rejoignent ce trimestre (26 % et 27 %), elle est devenue conditionnelle ; la phrase sur l'actionnariat citait un porteur à 28 % comme preuve que « les maisons mères et les États gardent leurs titres », elle ne cite plus qu'au-delà de 40 % ; et une valeur qui s'arrondit à zéro ne s'affiche plus « -0,0 % ». **Un bug hors de la note** : la clé « Cours » était traduite deux fois et la mauvaise gagnait, l'application anglaise affichait « Course » au lieu de « Price » sur la fiche d'une société, la page de l'indice, Titres et la leçon (`en-parcours.ts`). Le bloc étroit de `note.module.css` est passé après les règles de base : `position: static` sur la marge ne s'appliquait pas au téléphone. **Le lecteur de bulletin, réparé, et les bulletins à relire** (23 septembre 2026). En testant la règle de pondération de l'indice, la reconstitution butait sur des séances entières sans aucun cours d'action. Cause : jusqu'à novembre 2025, le PDF sortait la ligne d'une action sur trois lignes (cours précédent, date de séance, puis le reste) là où il les colle aujourd'hui sur une seule ; la fenêtre de recollage de `parseBoc` ne s'ouvrait que sur deux lignes, donc la date arrivait sans le reste et la ligne était déclarée « dense non reconnue ». **31 séances** étaient restées sans le moindre cours, de septembre à la mi-novembre 2025. La fenêtre passe à quatre lignes, ce qui couvre la mise en page d'aujourd'hui comme les trois variantes d'avant. Second défaut : une page de capitalisation présente mais illisible ne levait aucun avertissement, et **18 séances** la perdaient en silence ; elle en lève un désormais. Fixture et test de non-régression : `src/lib/market/__fixtures__/BOC-20250903.txt` et `boc-parse-legacy.test.ts` (BOC n° 2335, le bulletin à ligne éclatée). **Relecture** : les 77 bulletins incomplets ont été repris depuis l'adresse d'origine gardée sur chacun, sans un seul échec ; 31 séances gagnent leurs six cours, plus aucune séance n'est sans cours d'action, les statuts passent de 204 ok / 60 partiel à 232 ok / 32 partiel, et les séances portant une capitalisation de 216 / 234 à 244 / 265. Effet sur l'indice : la reconstitution par le flottant passe de 42 / 49 à **50 / 57**, et les séances où l'indice bouge sans qu'aucun cours lu ne change tombent de 14 à **6**. **Santé › Bulletins à relire** (`/desk/sante#relire`) ferme la boucle : le lecteur marquait ces séances depuis un an et rien ne le disait. Un point de Santé compte l'arriéré sur toute l'histoire (rouge dès qu'une séance n'a aucun cours, orange sinon), et un panneau les liste avec ce que le lecteur en a dit ; « Relire les plus anciens » en reprend six, « Relire » en reprend une (`bulletinsToReread` dans `src/lib/health.ts`, `rereadAction` et `Reread.tsx` dans `src/app/desk/sante`). À relancer après chaque correction du lecteur : un lecteur réparé ne rattrape pas le passé tout seul. **Un risque reste ouvert** : 6 bulletins sur 264 seulement ont leur PDF conservé au Dépôt, donc la capacité à relire dépend du maintien des fichiers sur le site de la BVMAC (`ingestBoc` prend déjà `keepPdf`). **Le contrôle de l'indice reconstitue, au flottant** (23 septembre 2026). Le point de Santé « Indice BVMAC et cours d'actions » ne comparait que des sens : l'indice bouge-t-il quand un cours bouge, et l'inverse. Il ne pesait aucune valeur, donc il ne pouvait pas voir qu'une variation publiée était six fois trop grande. `indexCheck` (`src/lib/market/index.ts`) garde les deux contrôles de sens, puis reconstitue la variation publiée avec les **poids de flottant de la séance** et dit « X publié, Y reconstitué au flottant » ; hors de la bande 0,8 à 1,25 le point passe à l'orange avec le facteur d'écart, et le sens contraire est nommé. Quand le flottant n'est pas lu sur la séance, il le dit plutôt que de deviner. Le flottant est la pondération vers laquelle la série pointe : sur les séances lues, 50 sur 57 s'y reconstituent, contre 18 à la capitalisation globale. Cela reste une inférence tant que la BVMAC n'a pas confirmé sa méthodologie, et l'écart est donc présenté comme une séance à éclaircir, jamais comme une erreur de la Bourse. Tests : `src/lib/market/index-check.test.ts`. **Les lignes publiées, confrontées au bulletin à chaque séance** (23 septembre 2026). Sur ce que le Guichet copie, la confrontation ne trouve rien : 0 cours différent, 0 date en retard, 0 ISIN manquant, 0 instrument faux, sur 42 lignes cotées et 45 OPCVM. Le défaut était ailleurs : rien ne remarquait qu'une ligne quitte la cote. Quatre obligations 2026 restaient publiées et commandables alors que le bulletin ne les cote plus, trois échues depuis juin et août, une absente depuis mars. `src/lib/market/reconcile.ts` compare les deux sens à chaque séance lue (sortie de cote, ligne du bulletin non publiée, cours, date, instrument, doublon) ; une absence ne compte qu'au bout de cinq séances, parce qu'une valeur peut manquer une séance et revenir. **Une ligne qui quitte la cote est clôturée, pas retirée.** `matured`, jamais `withdrawn` : « retirée » serait une décision de la maison, et ce n'en est pas une. Le client lit « Clôturée », qui dit notre position sans prêter aucune conduite à la BVMAC ; la page reste consultable avec ses documents et son historique de cours ; aucun ordre ne peut plus y passer ; et **rien ne lui est dit de la raison**. Le contraire ferait dire à la plateforme « cette ligne a été retirée du Guichet », ce qui est faux, ou « remboursée à l'échéance », ce que nous n'avons pas observé : le bulletin dit ce qui se cote, pas ce qui a été payé, et le remboursement se constate au dépositaire et par l'avis de remboursement. Le lecteur clôture seul une sortie de cote dont l'échéance est passée, et seulement en lisant la séance la plus récente : une relecture d'archive ne clôture rien. **Santé › Lignes et bulletin** liste les écarts avec, pour chaque sortie, la provenance de l'échéance (fiche signalétique de l'émetteur, ou estimée d'après l'intitulé et non confirmée), le **nombre de porteurs** (`positionsFrom`) qui décide de l'urgence, et un « Clôturer » pour celles que l'échéance ne tranche pas. Tests : `src/lib/market/reconcile.test.ts` et `src/lib/domain/line-close.test.ts`. **« Sur cette page », sur le public, et la famille du marché** (23 septembre 2026). La page de l'indice n'était rattachée à aucun onglet : sur /indice, rien ne s'allumait, ni sur ordinateur ni sur téléphone, et on n'y arrivait que par le pouls, présent sur deux pages. Trois réparations. (1) `/indice` et `/emetteurs` rejoignent l'onglet Sociétés, et /indice l'onglet Titres du téléphone : la page cesse d'être orpheline. (2) `src/components/PageOutline.tsx` porte enfin côté public le « Sur cette page » que le desk avait déjà sur sa documentation : rail collant à droite sur ordinateur, repérage de la section lue au défilement, et sur téléphone la ligne `SectionLine` (« Sur cette page · Le niveau · 1 / 7 ») qui ouvre la liste en feuille. Pas de liste déroulante : une liste déroulante cacherait la structure derrière une touche, la ligne la montre en 49 px. Posé sur la page de l'indice, dont les sept sections portent désormais une ancre. (3) `src/lib/market/pages.ts` déclare la famille du marché une fois (indice, sociétés, notes, comparer, leçon, avis) et `src/components/MarketStrip.tsx` pose « Sur le même sujet » au pied de chaque page de la famille, la page courante retirée. Une seule table, parce que des liens écrits à la main dérivent : c'est ainsi que la page de l'indice s'était retrouvée sans rien qui pointe vers elle. **Ce qui n'a pas été fait, volontairement** : lister dans un volet les liens que cite une page. Un lien dans une phrase porte sa raison, le même lien dans un volet n'est qu'une étiquette ; et un volet dont le contenu varie d'une page à l'autre ne s'apprend jamais. Les liens du texte restent dans le texte. Le rail ne porte que les sections ; la famille est au pied ; les deux se distinguent. **L'onglet Marché** (23 septembre 2026). L'environnement BVMAC avait des pages partout et aucune porte : l'indice n'était rattaché à aucun onglet, « Sociétés » était mince (sept sociétés) et « Actualités » menait à une page vide (2 liens reçus, 0 publié depuis un an, faute de relecture, pas faute de collecte). **Marché remplace les deux** : aucun siège ajouté, un libéré sur ordinateur. La bande passe de Titres · Fonds · Sociétés · Actualités · Guide à **Titres · Fonds · Marché · Guide** ; sur téléphone, Marché prend la place d'Actualités et Titres cesse de revendiquer /societes, /emetteurs et /indice. `/marche` (`src/app/marche/page.tsx`) est une porte, pas une page de plus : le pouls de l'indice, les sept sociétés avec leur cours et leur poids, les notes trimestrielles, « Comprendre » (la leçon, Comparer, ce que publie le bulletin), et les avis de la Bourse avec un vide honnête tant que rien n'est publié. Chaque section mène à la page qui existe déjà. L'ourlet de la page de l'indice, « BVMAC · indice de prix · lu à chaque bulletin », se lisait comme un fil d'Ariane sans être cliquable : « BVMAC » mène désormais à Marché, qui est le lieu qu'il désignait. **Ce que cela laisse ouvert** : les avis reçus attendent toujours une relecture, et le contrôle de Santé ne les compte qu'au-delà de sept jours. **La table des séances ne s'écrase plus, et la page respire** (23 septembre 2026). En portrait, les cinq colonnes de chiffres tenaient leur largeur (`.num` porte `white-space: nowrap`) et les deux colonnes de texte se repliaient en cinq lignes : la table n'a jamais défilé, elle s'écrasait, et `.tableWrap` ne servait à rien. **Une seule table partout** : « Ce qui a bougé » ne se replie plus, donc la table défile horizontalement, et tourner le téléphone en rend les colonnes au lieu de changer de présentation (1 430 px de table dans 345 px de cadre, vérifié). La colonne de la séance est collante à gauche, avec son fond, pour qu'on sache toujours quelle séance on lit en défilant vers les chiffres ; un dégradé aux deux bords dit qu'il reste des colonnes. **La variation passe en deuxième**, devant le niveau : une fois la table défilante, les colonnes de gauche sont les seules toujours vues, et la variation est le propos de la table quand le niveau n'en est que la somme courante. **Le rythme** : titres de section à 1,32 rem, 38 px entre les sections, `scroll-margin-top` pour que le titre ne se cache pas sous la ligne collante, et « Comment le lire » prend le fond or, seule section qui explique au lieu de compter (un fond alterné aurait dit que les sections sont de deux sortes, ce qui est faux). Le saut vers une ancre glisse (`scroll-behavior: smooth` sur `html`, sous `prefers-reduced-motion: no-preference`). La ligne « Sur cette page » devient **collante** sous 900 px, où il n'y a pas de rail : c'est le seul repère de section en portrait comme en paysage. Et `BackToTop`, qui existait déjà, est enfin posé sur cette page. **Les actualités sont un produit à part** (23 septembre 2026, correction). Marché avait avalé les actualités sous le titre « Les avis de la Bourse ». C'était une erreur de cadrage : les rubriques sont **Trésors, BVMAC, Sociétés, Fonds, Réglementation** — la BVMAC n'en est qu'une sur cinq — et la collecte tourne seule (`/api/cron/actualites` chaque jour, `/api/cron/actualites-hebdo` le vendredi), indépendamment de ce que nous publions. La section des avis disparaît de `/marche`, la bande « Sur le même sujet » dit désormais « Actualités du marché » et non « Les avis de la Bourse », et **Actualités retrouve son onglet sur ordinateur** : Titres · Fonds · Marché · Actualités · Guide, cinq onglets comme avant le renommage, avec l'indice qui a désormais une porte. **Sur téléphone, rien ne change** : cinq places pour six destinations, et Marché continue de couvrir /actualites. Le siège attendra la décision sur la relecture hebdomadaire, parce qu'un onglet de téléphone est une promesse de fréquence : un fil non relu la trahit en silence, alors qu'un fil relu sans onglet ne coûte qu'un détour, et se voit. Vide parce que personne ne relit n'est pas vide parce que rien n'arrive : deux avis attendent dans « reçu », et le contrôle de Santé ne les comptera qu'au-delà de sept jours. **Santé compte toute la file des actualités** (23 septembre 2026). Le point ne comptait que les liens reçus depuis plus de sept jours : une file fraîche restait invisible, et « personne n'a trié » se lisait exactement comme « rien n'est arrivé ». Il compte désormais **tout ce qui attend**, dit depuis combien de jours le plus ancien patiente, et ne passe à l'orange qu'au-delà de sept jours : voir la file est normal, la laisser vieillir ne l'est pas. C'est l'instrument du mois d'essai sur la relecture hebdomadaire : sans lui, l'absence de relecture ne se distingue pas de l'absence de collecte.

## 9. Variables d'environnement (Vercel › Settings › Environment Variables)

`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `AUTH_SECRET` (sel des codes de preuve, des liens signés et des codes à quatre chiffres : ne pas le changer sans raison), `CRON_SECRET`, `NEXT_PUBLIC_APP_URL` (adresse publique, dans les liens envoyés ; les clés d'accès se lient au domaine réellement servi), `DESK_EMAILS` (amorçage seulement), `DESK_MFA` (vide = obligatoire), `INBOUND_SECRET`, `INTAKE_TRUSTED_SENDERS`, `ANTHROPIC_API_KEY`, `RESEND_API_KEY` + `EMAIL_FROM`, `WHATSAPP_TOKEN` + `WHATSAPP_PHONE_ID` + `WHATSAPP_VERIFY_TOKEN`, `BOT_ENABLED`, `SETTLEMENT_BANK` + `SETTLEMENT_IBAN`, `NEWS_FEEDS` (flux RSS/Atom suivis en plus de celui de la BVMAC, séparés par des virgules), `VAPID_*` (notifications), `PHONE_OTP_*` (code par téléphone, facultatif), `OPENSANCTIONS_API_KEY` (facultatif). Voir `.env.example` pour le rôle de chacune. Le détail des services, des titulaires de compte et des coûts est dans desk › Documentation › « Plateformes, services et coûts ». Après un changement : *Redeploy*.
