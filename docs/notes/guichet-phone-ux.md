---
name: guichet-phone-ux
description: "Guichet on the phone (2026-09-19): gestures, cards, floating buttons, share image, card settings; the decisions the user took and the components that carry them"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8ecfdc15-a97a-41f6-a140-c3c9f48a4903
  modified: 2026-09-19T11:17:12.264Z
---

State on 2026-09-20 (master `cceb92c` and after, live on Vercel). One day of phone work with the user, decision by decision; the second paragraph is the current card pull, the first keeps the history.

**Gestures (decided by the user).** Swipe = the neighbouring line, on the fiche only: `src/components/mobile/SwipePager.tsx` wraps the fiche (`id` prop → neighbours from the sessionStorage list memory `guichet:listOrder`, which now carries `titles`); the drag follows the finger, the neighbour peeks in with its words stuck under the header (`.peekBody` sticky) and, past 30 % or a flick, says « Relâchez pour ouvrir », then `router.push` with `guichet:swipeDir` for the enter animation; first visit per device: a 38 px step aside + toast (`guichet:hint:swipe:fiche`, written when the toast shows so StrictMode's double effect does not eat it). The page swipe between lists (Titres ⇄ Fonds) was built, then **removed (F2)**: the cards own the horizontal gesture. `src/components/mobile/SwipeActions.tsx` first revealed Suivre · Comparer · Plus under a pulled card (touch only under 760 px), then the user found that redundant with « ··· »: see the next paragraph for what it does now. Long press was built then **removed** (redundant with « ··· »). Not done on purpose: pull-to-refresh (native on Android, none on iOS), swipe between the two market switches.

**The card pull, second version (2026-09-19 evening, `feb90dc` … `e0b4815`).** Suivre · Comparer · Plus left the pull (redundant with « ··· »). Pull left = « Déclarer » (fiche form, `#intention`) and « Me rappeler » (`?intent=rappel#intention`), a « ‹ » handle, a long pull fires Déclarer. Pull right = a 3D turn (`rotateY`, a width of pull = half a turn, a quarter turn commits) to a light-grey back (the user chose « dos clair »): `src/components/mobile/CardBack.tsx` = calendar with current step or dated facts (from `src/lib/domain/back.ts`, pure: `backFacts(offer, now)`, `fundBackFacts(row)`), the curve fetched on first turn by the server action `lineCurve(id)` in `src/app/offres/[id]/actions.ts` (60 closes / 60 NAVs / ≥3 past auctions of the same Treasury), and the fiche's reference calculation; compact view adds the list's four figures on top. Both the wrapper and the turning box take the back's height (the back is absolute inside the turning box: sizing only the wrapper clipped it), a ResizeObserver follows the curve's arrival, the CardBack effect must not depend on its own state (it cancelled its own fetch once). Leaving: slide back 40 px from anywhere, tap the back or the handle, tap elsewhere, scroll. Mockups: https://claude.ai/artifact/DFXLdfvUkxWrCLtus4V3Me (what to put under a pull), https://claude.ai/artifact/Kj3X47EcihAMuVeD5DLYwc (the back), https://claude.ai/artifact/PfLS5uYcZc3o3tyg668CvU (C, D, E by instrument).

**The back, third pass (`cc47642`, `d79c49c`, `cceb92c`).** Pale blue (`--info-soft`, border `--info`): the light grey read as the front. Colours on the back must follow the theme: the curve is `var(--chart-out)` (navy stays navy in dark mode and vanished on the dark back; the user's phone is in dark mode), dates `--ink-2`, rails and dots `--info`. A turned card is being read: it comes back only on its « ↺ Recto » button (`data-recto`), a deliberate half turn back (angle < 90 from the back), when another card turns (module-level `turnedNow`), or once it scrolls out of sight (IntersectionObserver, rootMargin -60/-56 px, threshold min(0.6, room/height·0.8)); tapping around it does nothing. The actions panel keeps its easy exit. Performance: at rest a card is a plain box (no perspective, no preserve-3d, no will-change, back `display: none`); `.live` is added on touch and removed 300 ms after rest. Fifty always-composited cards crashed Safari on the user's iPhone (« A problem repeatedly occurred »). The wrapper's ResizeObserver must observe every child of the back (the Recto button hid the content once).

**Sign-in link.** An expired or already-used magic link never reaches /auth/callback: Supabase redirects to the site root with `#error=…otp_expired`. `src/components/AuthHashRedirect.tsx` (in the layout) turns it into `/connexion?erreur=lien`, and the sign-in page shows a plain notice. iOS Mail previews consume one-time links: prefer the 6-digit code.

**Presentation.** `src/components/mobile/Presentation.tsx`: « Guichet en trente secondes », five screens (SCREENS data + Art1…Art5 SVG components animated by CSS classes), 6 s each, story under 760 px (bars, tap right two thirds = next, left third = back, hold = pause, « Passer », last screen waits with « Commencer »), two columns above (drawing + four steps, ← →). Shown once per device (`guichet:presented`) before Onboarding, which now waits for any open `[role=dialog][aria-modal=true]`; `ReplayPresentation` on the Guide (also `?presentation=1`, prop `fromQuery`) and under the sign-in form. Mockups: https://claude.ai/artifact/V5QakPwVECBDxjZQDdXcdA (A/B/C), https://claude.ai/artifact/JZbCUcuevB87FLYrjH5nQy (A+B).

**Discussed, not built: PIN / biometrics after email + WhatsApp are validated.** Recommendation given 2026-09-20: passkeys (WebAuthn) first, a device-bound 4-digit PIN as fallback, OTP underneath and required again for a new device, a changed phone/email, a first large intention, and always for the desk MFA; a « Sécurité » screen in Mon espace (trusted devices, revoke, change code); challenges: web storage (cleared data = back to OTP), shared phones, Supabase has no native passkeys (own tables + routes, @simplewebauthn), recovery through the desk. Next step if the user says go: mock the sign-in and Sécurité screens.

**The « ··· » (LineMenu).** `src/components/mobile/LineMenu.tsx`: three bold gold dots, nothing around them (the user rejected a bordered chip, then a gold chip); the sheet has a navy head with the title and the figure in gold, gold icon chips, darker scrim; actions: Voir la fiche · Suivre/Ne plus suivre (toggleWatch; not signed in → /connexion) · Comparer avec… · Partager le lien (navigator.share, else copy) · Copier l'ISIN (clipboard, execCommand fallback, then the value as a toast; never `prompt()`, the pane forbids it) · Fiche PDF on the fiche. « Déclarer une intention » is never in the menu. `openRef` lets the card's pull open the same sheet. On cards it sits in the top-right corner under the status; on table rows beside « Voir la fiche »; list rows get it absolutely on the corner (a button cannot live inside the row link).

**Cards (layout D).** `src/components/OfferCard.tsx` and `src/app/fonds/FundCard.tsx` (phone only, `usePhone`; the funds table stays on a desk), both on `OfferCard.module.css`: corner = status + dots, the number, three facts with an optional note under the value (`OfferSummary.facts` is `[label, value, note?]`: the duration under the maturity, the inception date under « depuis l'origine »), foot = closing date + « Voir la fiche → ». No coloured top edge by default. `src/components/Density.tsx`: density (detail | compact, `guichet:cartes`) and distinction (`guichet:cartes:sep`, default `zebra`; none · zebra · half · full · left · tint · shadow · zebrahalf) chosen in the « Affichage des cartes » sheet beside the density icons; lists set `data-sep` on their container; the rules are at the end of OfferCard.module.css (a CSS-module rule needs a local class, so the container gap for « shadow » lives in the browsers' CSS). Compact = title + [status ···], then number · condition · date.

**Lists.** Nothing frozen any more on Titres or Fonds (the user unfroze both): once the toolbar passes under the header, `src/components/FilterFab.tsx` (round icon, bottom-left, portaled to body, count badge) opens the same controls in `src/components/mobile/Sheet.tsx` (bottom sheet on the phone, dialog on a desk; the Titres FilterSheet gained the search and the sort). `src/components/BackToTop.tsx` bottom-right on Titres, Fonds, Guide, parcours: hysteresis (past 0.8 screen, hidden under 0.3), no backdrop blur (both flickered on the phone); both corners are 36 px, white, one thin line, the user found the navy/gold version intrusive. Each list keeps its own memory `guichet:listScroll:<pathname>` {url, y}, written as the reader scrolls (timer throttle, not rAF), ignored once the address moved on; `useListScroll` re-applies the saved position for one second against the router's scroll-to-top. A fixed element inside the pager's `will-change` ancestor sits in the page, not the viewport: keep BackToTop and the FAB outside or portaled.

**Share image.** `src/app/offres/[id]/opengraph-image.tsx` (ImageResponse, Manrope fetched from Google Fonts at run time with a plain fallback, glyphs outside the font drawn as divs) + `generateMetadata` on the fiche writes og/twitter title and description with the figures. WhatsApp caches previews; add `?v=1` to force a fresh one.

**Browser-pane gotcha.** A hidden pane stalls requestAnimationFrame, so React 19.2's batched Suspense reveal (`$RB`/`$RV`) never runs and a streamed list (OfferBrowser under `<Suspense>`) looks blank with its content in `<div hidden id="S:0">`. Take a screenshot to bring the pane forward, then test. Not a bug.

**Docs.** The aide Q&A « Quels gestes sur le téléphone ? » and OPERATIONS §4 « Côté client, sur téléphone » describe the current gestures (fiche swipe, card pull left = Déclarer · Me rappeler, pull right = the back, « ··· », card settings); update both when a gesture changes.

**Guide shots.** Regenerated 2026-09-19 night (`a379dc7`, 43 shots; the carte-dos and fonds-dos shots predate the blue back and the Recto button: re-run `ONLY=carte-dos,fonds-dos`): `GROUP=desk` on the seed (`node scripts/dev-memory.mjs`, port 3000: Next 16 refuses two dev servers in one project dir, so stop the Supabase one first), `GROUP=client` against `npm run dev` on Supabase (real lines and funds); nine phone shots (titres-phone, titres-compact, carte-actions, carte-dos, fonds-phone, fonds-dos, menu-ligne, affichage-cartes, fiche-phone) driven by synthetic touches, listed under Marché in `src/data/desk-guide.ts`. **Still to do.** The bypass/auto permission mode could not be set from the session (refused); the user sets it in the app.

Mockups (design log): https://claude.ai/artifact/5fRfPKJiEGd1G4aGtwF9Hc (gestures), https://claude.ai/artifact/EQh6WLzASs9AyfpPExm54C (lists), https://claude.ai/artifact/DinaEbRqEr2GX6QzdDwEG6 (dots + share preview), https://claude.ai/artifact/ETBKxNESE3Qn2h5arCuwBS (cards, swipe), https://claude.ai/artifact/JEMx3pFaDA2cRTMtYq5XSK (distinction). See [[guichet-project]], [[guichet-docs-and-guides]].

**The app « ⋮ » (2026-09-19):** `src/components/AppMenu.tsx`, mounted in both headers (layout `.right` and `MobileShell` right slot); vertical white dots = the app, gold horizontal ··· = a line. Sheet with `navy` + `dock="top-right"` props (a bottom sheet on the phone, a menu under the header on a desk). Groups: Parler à quelqu'un (wa.me prefilled with document.title on a fiche, tel:, mailto:), Découvrir (replays the page's CoachMarks through the `guichet:coach:replay` event and the `data-coach-replay` label; Presentation / Onboarding with `force`; desk: startDeskTour, `/desk/guide#<GUIDE key of the current path>`, docs, « Ce que voit le client » = presentation then onboarding), Réglages / Mon compte (LangSwitch compact, `CardDisplaySheet` exported from Density.tsx, PushToggle compact, Sécurité with counts computed in layout, logout form), footer with licence + build sha. One-time coach mark on the ⋮ (`guichet:coach:menu`) once `guichet:presented` and `guichet:onboarded` exist. Mockup: https://claude.ai/artifact/8hbyqNffExqZS139puvpmM. Not built yet: the gold dot + « Le desk vous a répondu » (needs in-app messages).

**2026-09-19 evening fixes:** card back = ivory `--back-face` with gold edge (blue was too subtle); zebra `--zebra` and shadow stronger; tab labels `--ink-2` 700; a pull may start on the line's name link (SwipeActions ignores only button/input/select/[role=slider]) and the click after a pull is swallowed; lesson pages wrapped in `SwipePager` (prev/next lesson, next section after the last); parcours sections lettered A–F with `letter()` in Parcours.tsx, all fold (`guichet:parcours:open` = "" means none), section colours are literal hexes readable on both grounds, `SectionShape` has `diamond`; `.reader` is `display:block` on phone so the chip nav sticks on Safari (a sticky grid item is caged in its grid area there); lesson diagrams and `Illustrations` use `--chart-out` instead of `--navy` (dark mode); glossary one column with wrapping chips on phone; the Guide's two replay buttons moved to the ⋮. Sixth parcours section `economie` (8 lessons, `src/data/lessons-economie.ts`). Mockups to build next: https://claude.ai/artifact/WKGLgP755cLt4sQEHvoh5k (⋮ redesign A/B, Aide page, Guide bar + sommaire sheet, financial profile).

**2026-09-20, ⋮ with two tabs.** On the phone `AppMenu` has tabs « Guichet » / « Contact » at a fixed sheet height (`Sheet tall`, foot pinned to the bottom with `margin-top:auto`): Guichet = search, « Sur cette page », six tiles with icons (Trente secondes, Premiers pas, Aide, Le Guide with n/36 read, Mon profil with the kind word from the `profile` prop set in layout via `getFinancialProfile`, Sécurité with counts), a `.line` row with LangSwitch + Se déconnecter (or Se connecter), the alerts row; Contact = `.waCard` (navy, number large, « Écrire »), `.contactPair` (Appeler, E-mail), address + licence, hours with « en ligne maintenant » computed in `show()` (`deskOpenNow`, Yaoundé UTC+1, Mon–Fri 8–17). « Affichage des cartes » left the menu (Density.tsx keeps its own sheet on the lists). Desk keeps Aide / Réglages. Mockups: https://claude.ai/artifact/DGJdAbvyUSTvRtXtDfM3iC (MenuTabs, GuideHere, Double, DoubleMore, Illustration boards).

**2026-09-20, the « Sur cette page » line and « Filtres · n ».** `src/components/SectionLine.tsx` (phone only) is rendered by `ChapterLinks` (Outline.tsx) next to the desk chips, which `docs.module.css` hides under 760px: the line (eyebrow or coloured letter, current title, `n / total` or `n / m lues` when items carry `section` keys, chevron), the rail, and a navy Sheet listing sections (ticks, « ici », « Haut de page » / « Section suivante »). Pages pass `label` (Aide: « Sujet », parcours: « Section ») and `pageTitle`. `src/components/FilterLine.tsx` (phone only): « Filtres · n » + summary + « Tri » (hidden when `sortLabel` is undefined), opening the list's sheet: Fonds (existing sheet; `.deskTools` hides the in-page toolbar), Sociétés (new sheet, toolbar moved to a const), the shared `Toolbar` (Actualités, desk docs: `.deskBar` / `.sheetBar`). Titres already had « Filtrer · n ». House line: `a` in globals.css sets dotted 1px `--ink-3` offset 3px; every permanent `text-decoration: underline` became `underline dotted`; `TermWord` uses the same. The alerts row in the ⋮ menu renders only when `vapidKey` is set (production has none yet).

**2026-09-20 evening, ⋮ back to one sheet.** No tabs on the phone: search, « Sur cette page », tiles, the FR · EN line with Se déconnecter (or Se connecter), then a « Nous joindre » group (the former Contact tab as `contactBlock`), footer. Alerts row removed from the menu (Mon espace has the PushToggle). Desk keeps Aide / Réglages.

**2026-09-20 night: folds, palettes, account sheet.** `src/components/Fold.tsx`: `FoldSection {group,id,title,aside,hint,defaultOpen}` + `FoldAll {group,ids}`; state in localStorage `guichet:fold:<group>:<id>`, event `guichet:fold`; a hashchange to `#id` opens the section. Used on /info (group info) and /moi (group moi, hints = count + state word; MyDocuments takes `inFold`). Palettes: `src/app/palettes.css` (`[data-palette=ivoire|ardoise|encre]`, light + dark overrides), `PaletteSwitch` in the ⋮ (phone sheet and desk Réglages), `PALETTE_BOOT` inline script in layout head applies `guichet:palette` / `guichet:theme` before paint (`<html suppressHydrationWarning>`). Account: `src/components/mobile/AccountMenu.tsx` replaces the avatar link in MobileShell (props segment, tier, email from the session); rows to /moi, /moi/profil, /moi/securite, three « bientôt » rows inert. Later that night the sheet became the full account (rule: initial = you, ⋮ = app): editable name/city (`identityAction`, segment keeps its first word), channel rows with proof state, « Chez vous » incl. « Mes coordonnées et pièces » (KYC hint), « Préférences » (LangSwitch, PushToggle, reach WhatsApp·E-mail·Appel, statements by e-mail via `prefsAction` → `profiles.prefs`), « Bientôt » group, foot = logout this device + `logoutEverywhere`. The ⋮ keeps language + « Se connecter » only for visitors. Contrast audit of the 8 palette×theme combos done the same night; pale-on-navy texts use `--on-navy-2`, never a hex. Desk shows it via `src/components/desk/ReachLine.tsx` (client file + intent page). Colours persist via cookies + server-stamped `<html>` (`src/lib/palette.ts`: `paletteAttrs`, `PALETTE_BOOT`, `P_COOKIE`/`T_COOKIE`; `PaletteKeeper` re-applies after navigation): React resets `<html>` attributes on layout re-render otherwise. Themes: auto · clair · tamisé (`dim`, a light-inked blue-grey middle ground, NOT a lighter dark) · sombre; the auto-dark media selectors exclude `dim` via `:not([data-theme="dim"])`, and palette light blocks beat globals dim, so each palette has its own dim block re-setting surfaces. Tours: `startDeskTour` dispatches `guichet:desktour:start` when already on the first stop page; missing targets are told without a ring instead of skipped (DeskTour + CoachMarks). `--bar-edge` gold hairline under navy bars in dark/dim. Swatches are tiny pages. Fund categories `--cat-M/O/D/A`. List view: `usePhone()` decides cards/table at hydration (no compact flash), SSR table hidden on phone unless `?vue=`.

## Une feuille s'ouvre du bord où se tient ce qui l'ouvre (23 septembre 2026)

Règle écrite dans `src/components/mobile/Sheet.tsx` : le `dock` d'une feuille
suit le bord dont son déclencheur est le plus proche.

- L'onglet « Marché » est sur la barre du bas : sa feuille monte du bas.
- La pastille du Guide est dans le coin bas : la sienne aussi.
- La ligne « Sur cette page » est gelée en haut : sa liste tombe de dessous elle
  (`dock="under"`, avec `anchorTop`). Le haut se **mesure à l'ouverture**, pas en
  CSS : la ligne est à 200 px en haut d'une note, à 101 px une fois défilée. Le
  voile part du même bord, la ligne reste visible au-dessus de sa liste, et le
  glissé se renverse (on repousse vers le bord d'origine).
- Sans point d'accroche mesurable (le Guide cache cette ligne, il a sa pastille),
  la feuille remonte du bas comme avant.

**Exception assumée** : le « ⋮ » et l'initiale du compte se tiennent en haut à
droite et tombent de leur déclencheur sur ordinateur ; une requête média les
renvoie en bas sur téléphone. Le principe les désigne, la décision est de ne pas
y toucher : ils s'ouvrent des dizaines de fois par jour, en bas de l'écran où se
tient le pouce.
