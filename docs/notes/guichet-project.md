---
name: guichet-project
description: "Guichet (Purpose Capital brokerage app) — state of the build, decisions taken, what is left; repo at C:\\dev\\guichet, pushed to github.com/totostar1900/guichet, live on Vercel"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8ecfdc15-a97a-41f6-a140-c3c9f48a4903
  modified: 2026-09-18T23:25:21.164Z
---

Guichet = Purpose Capital's client-facing repertory of CEMAC opportunities (OTA/BTA auctions, BVMAC IPOs, buybacks, secondary market, OPCVM) with intents flowing to a desk; WhatsApp as key channel. Code lives in **C:\dev\guichet** (outside OneDrive), Next.js 16 + Supabase (project sernniidkjkwqromvmqu), pushed to https://github.com/totostar1900/guichet.git (origin master) → auto-deploys to https://guichet-seven.vercel.app (Vercel project purpose-capital/guichet; crons boc/coupons/emetteurs/suivi/point).

Decisions (user's): offers are read-only; **no commission shown to clients** (communicated on contact; commissionPct stored 0); nominative sub-accounts at the SVT; groupements allowed; light screening; nothing manual for BVMAC market data (daily BOC ingestion); funds only under a distribution agreement; yield is the hero figure with the par rule; no tax view for now.

Governance (2026-09-16, user asked for "product types, reference data and your solutions, non-stop"): product types + reference data editable in-app (desk › Référentiel, table `reference`, defaults in code); roles client / desk (opérateur) / responsable with mandatory TOTP MFA (`DESK_MFA=off` escape), desk › Équipe, `DESK_EMAILS` = bootstrap only; hash-chained immutable `audit` table (desk › Journal), full snapshots in `offer_versions` with restore (desk › lignes/[id]), optimistic locking (`expectedVersion`), four-eyes via delegated window + `approvals` (desk › Approbations); lifecycle with « en revue » intake state, withdraw / relist, never delete; inbound e-mail (`/api/inbound/email`, INBOUND_SECRET, postal-mime) and WhatsApp media from staff → À valider; OPERATIONS.md runbook. Migrations applied through 0025 (0025 = news table).

Desk & i18n (2026-09-18): desk nav in 4 groups (DeskNav.tsx), app-wide ui/Select + ui/Toolbar (no native <select> left), /desk/intentions/[id] (client 360 + order checks src/lib/domain/checks.ts), /desk/messages inbox (inbound_messages table, migration 0024), /desk/guide (src/data/desk-guide.ts, screenshots public/guide via `npm run guide:shots` against `npm run dev:memory`), DeskTour cross-page walkthrough. Bilingual FR/EN: dictionary keyed by the French text (src/i18n/en*.ts), `getT()` server / `useT()` client, cookie guichet_lang + LangSwitch; every page is covered (en.ts, en-desk.ts, en-content.ts, en-lessons.ts, en-rest.ts, en-prose.ts); an untranslated string just stays French. Convention: wrap new JSX text in t("…") (tr when a component already has a `t` prop) and add the English line to the fitting en-*.ts file. English audit: scratchpad script audit-en.mjs (headless Chrome, dev cookie, guichet_lang=en, French-stopword heuristic) over a pages JSON — last full run 2026-09-18 on 49 pages: every UI string clean; remaining French is data by design (names, seeded messages/feed, communiqué text, glossary source tab, desk-written news titles, bilingual notes README). Fallbacks in core.ts: templates with {x}, segment split (· — ; . :), lowercase/period-less variants, French month/day tokens in data strings. Titles rendered through LineIdentity and document names on a fiche go through t(). Dates/numbers keep the fr-FR format in both languages. Apprendre merged into /info (redirects kept). Funds arrive distributed+visible (migration 0023). "À la une" lives inside OfferBrowser under the toolbar; yield filter is a histogram range gauge (YieldRange.tsx, URL rendement=min-max).

Actualités (2026-09-18): src/lib/news/* — items live in their own `news` table (migration 0025; RLS: anon reads only status=publiee, desk everything; repo listNews/upsertNews/deleteNews), public /actualites + /desk/actualites (Marché group), links in via form / WhatsApp from a staff phone / trusted e-mail, crons /api/cron/actualites (04:00, feed watch BVMAC + NEWS_FEEDS env + link check) and /api/cron/actualites-hebdo (Fri 16:00, notifyRaw kind digest); i18n dictionary en-news.ts; the desk writes titleEn/whyEn by hand or English readers see the French. Sites behind Cloudflare (cosumaf.org) answer 403 to bots: treated as alive. Robot/e-mail link intake (src/lib/news/intake.ts); crons 04:00 daily + Fri 16:00; 7 crons total ⇒ Vercel Pro needed. Documentation/guides/tours: see [[guichet-docs-and-guides]]. OPERATIONS.md updated 2026-09-18 with news, docs, guide, cron list, env vars, notes page, client maintenance chapter, guide galleries (aide-entretien capture). Memory notes are mirrored in the repo at docs/notes and rendered on /desk/docs/notes.

Client navigation (2026-09-18 evening): lists remember themselves — `src/components/ListNav.tsx` (sessionStorage `guichet:listOrder` = {url, ids, label} written by OfferBrowser/FundsBrowser, `guichet:listScroll` saved on the click that leaves for a line and restored with three timed scrollTo calls because the router scrolls to top after mount); the fiche shows `<ListNav>` (back to the exact list + previous/next in that order). FundsBrowser filters live in the URL (q, cat, gestion, vl, tri, sens); its selector band is sticky with fixed-width selects. Fund chart = client `NavChart.tsx` (hover/touch bubble; SVG `<title>` must be a single string or hydration fails) inside `NavPeriod.tsx` (2026-09-19: duration tiles 1m…3a/origine or du/au dates snapped to the nearest published NAV, URL params `periode`/`du`/`au`, default 1a when the history allows; stats block on the window; phone: coach marks wait for open dialogs, Actualités is a bottom tab, Guide/Aide panes are chapter chips). Guide (`/info`, the tab was renamed from « Info » to « Guide » in both languages, route unchanged) and Aide (`/info/aide`) use the docs reader layout (`docs.module.css` .reader.readerTwo, nav sticky at top 72px under the 60px header, phone = one column via `.reader.readerTwo` specificity) with `ChapterLinks` scroll-spy; the right Outline was removed from Aide. InfoSearch: STOP words drop question filler, stems, SYN map of everyday words, results in the flow, `revealAnchor()` scrolls + adds `.flash` (global `:target, .flash` animation in globals.css, `html{scroll-padding-top:76px}`). Compare page: client `LinePicker` per side (search + market + instrument chips, auto-submit when both set) + `BackButton`. SEGMENT_LABEL.fonds = "Gestion collective" → EN "Mutual funds" (user's wording, 2026-09-19; "OPCVM" also → "Mutual funds"). Market navigation = proposition A, built 2026-09-19 (mockup https://claude.ai/artifact/K6w4qaxYhw4Ywsu3nP7q8j): top menu « Titres » (ex-Guichet tab, route /) and « Fonds » carry counts (layout.tsx countOffers → NavTabs); `MarketToggles.tsx` = TitresHead (eyebrow, h1, « Voir les {n} fonds ») outside the sticky band + two switches (URL `marche` absent = both on; the last one off ⇒ both back on); MarketTabs.tsx deleted, Fonds page has no strip. Funds page is one flat table with a foldable « Quatre catégories » band (localStorage guichet:fonds:familles) and the category paragraph in lesson fonds-vl.

Fund/compare charts (2026-09-19): `FundCharts.tsx` (rollingAnnualised / invested / changes / drawdown, unit-tested in src/test/fund-charts.test.ts) drawn inside `NavPeriod.tsx` by `?graphe=` + `?fenetre=`; the fiche loads the whole NAV history (listFundNavs 2000) and passes the latest BTA yield as benchmark. Compare page: `lib/domain/compare.ts` (compareLine → return nature promesse/passe/cours, flows for debt/shares/buybacks, navs for funds) + `CompareCharts.tsx` (ReturnBars for every pair, TwoFunds, FundVsDebt with the promise drawn as steps = cash received + amount still held, Calendar with amount labels ≥ 25 % of the largest flow); every chart has a « Comment lire » box; texts are in-component, FR keys in en-rest. Axis: `axisLabel()` in NavChart.tsx (6 dates, 5 with the year over > 366 days). Chart overlap rule: benchmark labels go in an HTML legend row, never in the SVG; left margin = label length × 3.6 + 8.

Compare page (2026-09-19): a fund's return is two rows — « Rendement » (12 months or per year since inception) and « Depuis l'origine » (cumulative + launch date + age via tenorText, placed right under Rendement) — with Info bubbles vl / variation_vl / perf_origine (new glossary term in src/lib/glossary.ts, EN in en-content); value strings assembled from fragments need a template each (en-templates: "{n} titres", "1 bon", "Prix d'achat par bon (…)", "Pour {n} titres cédés au Trésor", "{n} Mds FCFA rachetés", "créé le {d} · {age}"). Titres page: the count · sort · views line sits above « À la une ». Design log for these screens = the canvas https://claude.ai/artifact/K6w4qaxYhw4Ywsu3nP7q8j (nav A, fund period, fund readings, compare graphs — all marked "construit").

Deployed state 2026-09-19 01:00: Vercel production = `4e77145` (everything above is live on desktop and phone — one site, one build). See [[guichet-push-after-each-step]] for the staged-promotion gotcha that hid the evening's pushes for an hour.

Still on the user's side: login on the live site (first desk login now enrols MFA), Resend + WhatsApp keys, Cloudflare Email Routing for the intake mailbox, custom domain name, GitHub branch-protection rule if they want PR gating (would change the direct-push workflow), distribution agreements, COSUMAF/BVMAC agreements.

**Why:** the user works from the OneDrive folder but the app must stay in C:\dev\guichet; the memory saves re-deriving the architecture each session.
**How to apply:** continue in C:\dev\guichet; run `npx vitest run`, `npx tsc --noEmit -p .`, `npx eslint src`, `npx next build` before each commit; see [[guichet-push-after-each-step]] and [[guichet-supabase-migrations]].
